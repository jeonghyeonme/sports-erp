# 도메인 사이클 — 권한관리

> **상태**: 초안 (2026-09-22). [process/03_도메인_사이클_템플릿.md](../process/03_도메인_사이클_템플릿.md)의 11단계를 적용한 1호 사이클.
> **대상 코드**: `apps/api/src/modules/auth/`, `apps/api/src/modules/permissions/`, `apps/api/src/common/guards/`
> **이 문서가 권한관리 도메인의 단일 문서다.** 옛 [1-2_권한관리.md](../1.spec/10_인사조직/1-2_권한관리.md)의 내용을 병합했다 — 사용자 스토리는 §0으로, 나머지 레퍼런스(데이터모델·화면·API명세·권한매트릭스·구현우선순위)는 부록 A로 옮겼다. 1-2문서는 아직 물리적으로 남아 있지만 **폐기 예정**(대체됨 배너 추가) — 6개 문서가 그 경로를 참조하고 있어 링크 정리가 끝나야 실제 삭제할 수 있다. 진행 상황은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md) 참고.
> **문서 순서 원칙**: 두 종류를 구분한다. ① **§0(사용자 스토리)는 원본 입력값**이다 — 우리가 내린 결정으로 바뀌지 않는, 만족시켜야 할 대상이라 맨 앞에서 독자에게 먼저 맥락을 준다(영상도 고객의 원문 요구사항을 먼저 쭉 나열한 뒤에 분류로 들어간다 — 같은 순서). ② **부록 A(데이터모델·API·화면·매트릭스)는 결정의 산출물**이다 — §1~12(요구사항 분류 → Driver → 대안 비교 → 결정)가 원인이고 부록은 그 결과 스냅샷이므로 맨 뒤에 둔다. §1~12의 결정과 부록 내용이 어긋나면 부록을 고친다(반대 방향 아님).
> **선행 관찰**: 이 도메인은 이미 상당 부분 구현되어 있고, 옛 §6은 "인증 방식 정정(2026-09-15)"이라는 제목으로 **대안 논의를 이미 산문으로 서술**해 뒀다. 이번 사이클의 상당 부분은 새 결정을 만드는 게 아니라, **이미 내려진 결정을 Driver·대안·ADR 형식으로 정식화**하는 작업이다. 이게 정상이다 — 영상도 "이미 아는 답을 이 형식에 넣는 연습"을 금지하지 않았다.

## 0. 이 도메인 개요 (사용자 스토리)

- 본사 관리자로서, 모든 지점 데이터를 조회하고 신규 지점 관리자 계정을 발급하고 싶다.
- 지점 관리자로서, 로그인하면 내 지점 데이터만 보이길 원하고 실수로도 다른 지점 정보에 접근할 수 없기를 바란다.
- 회원으로서, 내 정보와 내가 속한 지점의 프로그램만 보고 싶다.
- 본사 관리자로서, 직원이 퇴사하면 그 즉시 시스템 접근을 막고 싶다.
- 직원(트레이너 등)으로서, 로그인해서 내 근태·휴가·업무일지만 스스로 관리하고 싶다 — 다른 직원 정보나 회원 관리 화면은 볼 필요도, 보여서도 안 된다.

RFP는 "사용자별·프로그램별 세부 권한(입력/수정/삭제/조회/출력)"을 요구하지만, 위 스토리들의 실질(지점 간 격리·퇴사 즉시 차단·관리자/직원 분리)만 충족하도록 4-Role RBAC로 조정했다 — 조정 이유는 ADR-AUTH-03(§10).

## 1. 요구사항 재분류

### 기능
| ID | 내용 |
|---|---|
| F01a | 권한관리 기능은 관리자 계정에만 부여 |
| F01b | 사용자별·프로그램별 권한 설정(입력/수정/삭제/조회/출력) |
| F01c | 본사↔현장, 현장↔이용자 간 별도 권한 관리 |
| F01d | 퇴사 처리 이후 시스템 접근통제 |

### 품질
| ID | 내용 |
|---|---|
| Q11 | 접근 통제가 그룹별·개인별·기능별로 구체적이어야 함 |
| Q15 | 사용자 권한에 따라 버튼(수정/삭제/조회) 기능 제어 |

### 제약
- C01(과업 기간)이 간접 제약: 1인 개발 일정상 엔터프라이즈급 ACL을 만들 여유가 없다 — 아래 Driver 3의 판단 근거.

### 불변규칙 (협의 불가)
영상의 "동일 좌석 중복판매 금지"에 대응하는, 이 도메인의 협의 불가 규칙 세 가지:
1. **퇴사·탈퇴 처리된 계정은 즉시 접근이 차단된다.** (F01d, RFP 원문 그대로 — "몇 분까지는 봐준다"가 성립할 수 없는 성격)
2. **채용·재배치·Role 변경은 본사(SUPER_ADMIN)만 한다.** (CLAUDE.md 4대 불변식 — 인사 권한 분리)
3. **BRANCH_ADMIN·STAFF·MEMBER는 자기 소속 범위를 벗어난 데이터에 접근할 수 없다.** (CLAUDE.md 4대 불변식 — 지점 데이터 격리. 권한관리 도메인은 이 규칙을 *구현하는 메커니즘*의 근원이다)

## 2. 되묻기 확정

| ID | 질문 | 상태 | 확정 내용 |
|---|---|---|---|
| Q-07 | 퇴사 후 접근통제까지 허용 지연 | **이미 확정·구현됨** | 0초(같은 요청부터). ADR-AUTH-01(§10) 참고 |
| Q-17 | 관리자 범위 | **이미 확정·구현됨** | SUPER_ADMIN만 권한 설정. BRANCH_ADMIN은 본인 지점 STAFF 계정 생성/비활성화까지만 위임 |
| Q-18 | 권한 단위 | **이미 확정·구현됨** | 화면·기능 단위 4-Role RBAC. 사용자별·프로그램별 개별 ACL(F01b 문자 그대로)은 조정(축소) — ADR-AUTH-03(§10) 참고 |
| **Q-24 (신규)** | 동일 계정의 다중 기기 동시 로그인을 허용할까? | **미정 — 이 사이클에서 결정** | 아래 §4 Tier2 표 참고 |
| **Q-25 (신규)** | BRANCH_ADMIN이 같은 지점의 다른 BRANCH_ADMIN 계정을 비활성화할 수 있는가? | **미정 — 스펙 문구가 "직원"이라 모호** | 아래 §4 Tier2 표에서 가정으로 처리 |

## 3. 수치화

| 항목 | 목표 | 근거 |
|---|---|---|
| 퇴사 후 차단 지연 | 0초(다음 요청부터 100% 차단) | 불변규칙 1 |
| Role 변경 반영 지연 | 0초(재로그인 불필요) | ADR-AUTH-01, F01c 운영성 |
| 인증 미들웨어 오버헤드 | 응답시간 예산의 10% 이내(상대 기준) | 아래 §6 참고 — 절대 수치(ms)는 실DB 전환 후 실측(11단계로 이월) |
| 로그인 실패 잠금 | 5회 → 15분 | RFP 요구 아님, 이미 Phase2로 분류(부록 A-5) — Driver 아님, Tier2 |

**[공용 산정 상수](../architecture/design-constants.md) 사용 이력에 이 문서를 추가한다.** 다만 이 도메인은 "동시 재실자"보다 "초당 API 요청 수"가 중요한데, 그 배율이 아직 산정되지 않아 절대 수치 목표를 억지로 만들지 않고 상대 기준(응답시간 예산 대비 비율)으로 대체했다 — 공용 산정 상수 문서의 "약한 고리"에 이미 명시된 제약과 동일하다.

## 4. Architecture Driver 식별 (3개로 제한)

| # | Driver | 왜 구조를 바꾸는가 |
|---|---|---|
| D1 | 퇴사·권한변경 즉시 반영 메커니즘 | 캐싱 여부·조회 방식이 API 전체의 요청 경로(critical path)를 결정 |
| D2 | 위탁계약 종료 지점 소속 계정 처리 정책 | 자동/수동 여부가 운영 프로세스와 되돌리기 가능성을 결정 |
| D3 | 권한 모델의 세밀도(RBAC vs ACL) | 데이터 모델·화면 개수·테스트 조합 수 전체를 결정 |

**Driver로 승격하지 않은 것 (Tier2 — 규칙표로만 관리)**

| ID | 규칙 | 근거 | 처리 |
|---|---|---|---|
| AUTH-T01 | 로그인 5회 실패 → 15분 잠금 | 이미 Phase2 분류, RFP 비요구 | 구현: 여부 확인 필요. 미구현이면 "검증되지 않음" |
| AUTH-T02 | Refresh Token 1회용 rotate | 구현 디테일, 구조 안 바뀜 | 구현 완료(auth.service.ts 확인됨) |
| AUTH-T03 (Q-24) | 다중 기기 동시 로그인 | RefreshToken이 accountId당 여러 개 저장 가능한 구조라 이미 지원됨 — 정책만 "허용"으로 명시하면 끝, 재설계 불필요 | **가정: 허용.** 근거: RFP 언급 없음 + 회원·직원 모두 여러 기기(폰/PC) 사용이 자연스러움 |
| AUTH-T04 (Q-25) | BRANCH_ADMIN의 동료 BRANCH_ADMIN 비활성화 가능 여부 | 스펙 "본인 지점 직원 계정만"의 "직원"이 BRANCH_ADMIN을 포함하는지 모호 | **가정: 불가.** 근거: 동급 권한자를 서로 무력화할 수 있으면 인사 권한 분리 불변식이 흔들림 — 이건 본사(SUPER_ADMIN)만 |
| AUTH-T05 | 탈퇴 회원과 동일 이메일 재가입 | 스펙에 없음 | **가정: 허용, 신규 Account로 취급.** 근거: 개인정보보호법상 삭제 요청 시 기존 계정 완전 분리가 오히려 안전 — 이 사이클에서 발견, 회원관리 사이클에서 재확인 필요 |

## 5. Driver 우선순위

**불변규칙(§1) 항상 최우선** → D1(불변규칙 1을 직접 구현) → D2(불변규칙 3의 계약측 구체화) → D3(범위·일정 제약에서 나온 결정, 구조적이지만 리스크는 상대적으로 낮음)

## 6. 현행 구조 진단 (Baseline)

옛 1-2문서 §6이 이미 이 진단을 산문으로 해 뒀다 — 그대로 가져와 정식화한다: **정정 전(2026-09-15 이전) 구조**는 JWT를 순수 stateless로 검증(서명·만료만 확인)했다. 이 구조로는 퇴사 처리 이후에도 이미 발급된 Access Token이 **만료(최대 30분) 전까지 계속 유효**해, 불변규칙 1("즉시 차단")을 위반했다. 이게 D1을 만든 원인이다.

D1의 현재 해법(매 요청 Account PK 재조회) 자체의 baseline 리스크: 지금은 `MockDataService`(인메모리)라 조회 비용이 사실상 0이다. **실DB 전환 시 이 조회가 모든 API 요청의 critical path에 들어간다** — 이게 D1을 단순 "이미 끝난 일"이 아니라 계속 감시해야 할 Driver로 남기는 이유다.

## 7. 설계 질문 (Driver당 1개)

1. 퇴사·권한변경이 재로그인 없이 즉시 반영되어야 하는데, 어떤 방식으로 구현할까?
2. 위탁계약이 TERMINATED된 지점 소속 계정을 어떻게 처리할까?
3. 권한을 얼마나 세밀하게 모델링할까?

## 8. 대안 → 트레이드오프 → 결정

### 질문 1: 즉시 반영 메커니즘

| 대안 | 장점 | 단점 |
|---|---|---|
| A. Stateless JWT만(재확인 없음) | DB 조회 0, 성능 최고 | **불변규칙 1 위반**(최대 토큰 만료 시간까지 접근 가능) — 후보 탈락 |
| **B. 매 요청 Account PK 재조회 (채택, 현재 구현)** | 정확성 100%, 구현 단순, 파견 재배치도 동일 메커니즘으로 즉시 반영 | 매 요청 DB round-trip 추가(실DB 전환 시 latency·부하 증가) |
| C. 토큰 블랙리스트/revocation(Redis 등 외부 캐시) | DB 직접 조회보다 빠를 수 있음, 확장성 | 별도 인프라 신규 도입 필요(현재 스택에 없음), 캐시 무효화 타이밍 이슈, 인프라 하나 더 운영 |
| D. Access Token 초단명(예: 1분)+잦은 refresh | 인프라 추가 없음 | Refresh 검증도 결국 DB 조회 필요(RefreshToken 조회) + "최대 1분"은 불변규칙 1의 "즉시"에 못 미침 + 클라이언트 refresh 폭증 |

**결정**: B (현행 유지). 판단 기준은 불변규칙 1(A, D 탈락) + 현재 인프라 제약(C는 새 컴포넌트 요구, C01 일정상 정당화 안 됨). PK 인덱스 조회는 통상 수 ms대라 §3의 "응답시간 예산 10%" 상대 기준을 가설로는 만족한다고 본다.
**감수하는 것**: 실DB 전환 후 이 조회가 실제로 병목이 되는지는 아직 실측하지 못했다(11단계로 이월). 병목으로 드러나면 이미 스펙이 적어 둔 대로 "accountId 기준 수 초짜리 in-memory 캐시"로 완화한다(=C를 축소한 버전, Redis 없이).

### 질문 2: 계약 종료 지점 계정 처리

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 즉시 일괄 비활성화 | 규칙 단순·확실 | 과거 이력 조회·인수인계 업무까지 막음, 되돌리기(재활성화) 필요 시 수동 개입 필요 — 오히려 운영 부담 |
| **B. 쓰기 API만 차단 + 비활성화는 SUPER_ADMIN 수동 판단 (채택, 현재 구현)** | 비가역적 작업을 사람이 최종 결정, 다른 도메인(회원등록·예약·게시글)과 동일한 409 규칙 재사용 | 자동화가 안 돼 SUPER_ADMIN이 놓치면 방치 위험 |
| C. 유예기간 후 자동 비활성화(B + 스케줄러) | 방치 위험 완화 | 유예기간 값이라는 새 모호함 추가, 스케줄러 인프라 없음(다른 도메인의 노쇼 자동처리 등과 동일 사유로 이미 범위 제외됨 — 1-7문서 §8) |

**결정**: B (현행 유지). 판단 기준: "되돌리기 힘든 작업은 사람이 판단한다"는 이 프로젝트의 일관된 원칙(옛 1-2문서 §6에 이미 명시돼 있었음) + 스케줄러 인프라 부재.
**감수하는 것**: 방치 위험은 자동화가 아니라 운영 체크리스트(사업보고 절차 P03과 연결 가능)로 완화해야 한다 — 숙제로 남긴다.

### 질문 3: 권한 세밀도

| 대안 | 장점 | 단점 |
|---|---|---|
| A. RFP 문자 그대로(사용자×기능×CRUD 개별 ACL) | RFP 문구 100% 충족, 최대 유연성 | 엔터프라이즈 IAM 수준 — 1인 개발·C01 일정 초과 |
| **B. 4-Role 고정 RBAC (채택, 현재 구현)** | 구현 단순, Role×API 조합이 유한해 테스트 가능(hr-authority.spec.ts로 이미 일부 검증됨) | "사용자별" 세밀함 미충족 — RFP 문구와 괴리 |
| C. Role + 지점별 커스텀 오버라이드 | A와 B의 절충 | 오버라이드 규칙까지 테스트해야 해 조합이 다시 늘어남 — B를 고른 이유가 무효화됨 |

**결정**: B (현행 유지). 판단 기준: 기준②(지점 격리라는 RFP의 실질 핵심은 충족) + C01(일정) 제약.
**감수하는 것**: RFP 문구("사용자별·프로그램별 세부 권한")를 문자 그대로 만족하지 못한다 — 발표 시 "왜 조정했는가"를 원문 대비로 설명해야 한다(§0 개요에 이미 이 설명이 있음).

## 9. 감수하는 것 (종합)

- D1: 실DB 전환 전까지 "매 요청 조회가 병목이 아니다"는 가설로 남는다.
- D2: 방치 위험은 프로세스(사람)로 막는다 — 시스템이 보장하지 않는다.
- D3: RFP 문구와의 괴리를 스스로 설명해야 하는 부담이 있다(포트폴리오 발표 리스크가 아니라 오히려 "조정 판단력"을 보여줄 기회로 삼는다).

## 10. ADR

### ADR-AUTH-01: 퇴사·권한변경 즉시 반영 — 매 요청 Account 재조회
- **컨텍스트**: 불변규칙 1(퇴사 즉시 차단)이 stateless JWT로는 위반됨(§6 baseline)
- **검토한 대안**: A stateless만 / B 매 요청 재조회 / C 외부 revocation 캐시 / D 초단명 토큰
- **결정**: B. 불변규칙 위반(A·D) 및 인프라 제약(C)으로 배제
- **결과**: 좋아진 것 — 정확성 100%, 구현 단순 / 감수하는 것 — 실DB 전환 후 latency 실측 필요(가설 상태) / 숙제 — accountId 캐시 완화안을 실측 후 재평가

### ADR-AUTH-02: 계약 종료 지점 계정 — 쓰기 차단 + 수동 비활성화
- **컨텍스트**: 불변규칙 3(지점 격리)과 별개로, TERMINATED 지점 소속 계정의 취급을 정해야 함
- **검토한 대안**: A 즉시 일괄 비활성화 / B 쓰기 차단+수동 / C 유예기간 후 자동
- **결정**: B. "비가역 작업은 사람이 판단"이라는 프로젝트 원칙 + 스케줄러 인프라 부재(C 배제)
- **결과**: 좋아진 것 — 조회·인수인계 업무 방해 없음, 다른 도메인과 규칙 일관성 / 감수하는 것 — 방치 위험 / 숙제 — 운영 체크리스트화

### ADR-AUTH-03: 4-Role 고정 RBAC (개별 ACL 대신)
- **컨텍스트**: F01b가 사용자별·프로그램별 세부 권한(CRUD별)을 요구하나 1인 개발 범위를 초과
- **검토한 대안**: A 개별 ACL / B 4-Role RBAC / C Role+지점 오버라이드
- **결정**: B. RFP의 실질 핵심(지점 격리)은 충족, C01 일정 제약
- **결과**: 좋아진 것 — 구현·테스트 단순 / 감수하는 것 — RFP 문구 미충족(설명 책임) / 숙제 — 없음(확정 사항)

## 11. 검증 현황 (정직하게 보고)

| 항목 | 상태 |
|---|---|
| 채용·재배치·Role변경은 본사만 (불변규칙 2) | **검증됨** — `hr-authority.spec.ts` |
| BRANCH_ADMIN이 본인 지점 직원 일상관리 가능(대조군) | **검증됨** — 동일 파일 |
| **로그인된 상태에서 퇴사 처리 → 같은 토큰으로 즉시 401(D1의 핵심 주장)** | **검증됨(2026-09-22)** — `apps/api/test/auth-lifecycle.spec.ts` |
| **Role 전환 직후 재로그인 없이 새 권한 반영** | **검증됨(2026-09-22)** — 동일 파일 |
| D1의 latency/부하 가설 | **검증 불가**(MockDataService라 실측 무의미) — 실DB 전환 후 이월 |
| D2(TERMINATED 지점 계정 처리 정책) | **부분 검증** — `contract-termination.spec.ts`는 라우트(409) 차단만 확인, 계정 비활성화 정책 자체는 테스트 없음 |
| AUTH-T02(Refresh rotate) | 코드 확인됨(자동 검증 아님) |
| 로그인 5회 실패 잠금(AUTH-T01) | 스펙상 Phase2 미구현 — **미구현, 검증 대상 아님** |

**다음 행동 제안 — 완료(2026-09-22)**: D1의 "즉시 401" 시나리오와 Role 전환 즉시 반영을 `auth-lifecycle.spec.ts`로 검증했다(2건, 전체 테스트 스위트 통과 확인). 남은 것은 D1의 실측(latency) 자체뿐이며, 이건 실DB 전환 이후로 유효하게 이월된다.

## 12. 이월 사항 + 문서 마이그레이션 메모

- AUTH-T05(탈퇴 회원 이메일 재사용)는 회원관리 사이클(다음 순번은 아니지만 우선순위 상위)에서 재확인 필요.
- D1의 "동시 API 요청 수" 산정 공백은 예약및결제 사이클(정원 동시성)과 겹칠 가능성이 있다 — 그쪽에서 먼저 필요해지면 [architecture/design-constants.md](../architecture/design-constants.md)에 이어서 산정하고 이 문서를 갱신한다.
- 옛 스펙 문서와의 겹침 판정은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md)에 기록했다(이 문서에서 실행하지 않음).

---

## 부록 A. 레퍼런스 스펙 (결정을 반영한 현재 상태)

> §1~12(분석·결정)의 **결과물**이다. 옛 [1-2_권한관리.md](../1.spec/10_인사조직/1-2_권한관리.md)에서 병합했다. 여기 내용이 위 결정과 어긋나면 여기를 고친다.

### A-1. 데이터 모델

**Account**
| 필드 | 타입 | 설명 |
|---|---|---|
| id | UUID (PK) | |
| email | string, unique | 로그인 ID |
| passwordHash | string | bcrypt |
| role | enum(SUPER_ADMIN, BRANCH_ADMIN, STAFF, MEMBER) | |
| isActive | boolean, default true | false=접근차단(퇴사/탈퇴) |
| failedLoginCount | int, default 0 | 잠금 로직용(Phase2, 미구현) |
| lockedUntil | datetime, nullable | |
| lastLoginAt | datetime, nullable | |
| createdAt / updatedAt | datetime | |

인덱스: `email` unique, `role` index(관리자 목록 조회용)

**RefreshToken**: `id, accountId(FK), tokenHash, expiresAt, revokedAt` — 재발급/강제로그아웃 관리용. 토큰 원문 대신 해시 저장.

### A-2. 화면 목록
| 화면 | 대상 | 설명 |
|---|---|---|
| 로그인 | 전체 | 이메일/비밀번호, 5회 실패 시 잠금 안내(Phase2, 미구현) |
| 계정 목록/발급(관리자) | SUPER_ADMIN | 지점 관리자 계정 생성/비활성화 |
| 권한 관리(Role 전환) | SUPER_ADMIN | STAFF ↔ BRANCH_ADMIN 전환. 재로그인 없이 다음 요청부터 즉시 반영(ADR-AUTH-01과 같은 메커니즘) |
| 비밀번호 변경 | 전체 | 로그인 후 |
| (선택) 비밀번호 재설정 | 전체 | 이메일 링크 방식 — Phase 2, 미구현 |

### A-3. API 명세
| Method | Endpoint | 권한 | 설명 |
|---|---|---|---|
| POST | /api/v1/auth/login | 공개 | email/password → accessToken, refreshToken |
| POST | /api/v1/auth/refresh | Refresh Token | 토큰 재발급 |
| POST | /api/v1/auth/logout | 인증 필요 | Refresh Token revoke |
| GET | /api/v1/auth/me | 인증 필요 | 내 정보(role, branchId 포함) |
| PATCH | /api/v1/auth/password | 인증 필요 | 비밀번호 변경 |
| POST | /api/v1/accounts | SUPER_ADMIN | 지점 관리자 계정 생성 |
| PATCH | /api/v1/accounts/:id/deactivate | SUPER_ADMIN, BRANCH_ADMIN(본인 지점 직원만) | 접근 차단 |
| GET | /api/v1/accounts | SUPER_ADMIN | 전체 계정 목록 |
| GET | /api/v1/permissions/staff | SUPER_ADMIN | 전 지점 직원 목록 + 현재 Role 조회 |
| PATCH | /api/v1/permissions/staff/:staffId/role | SUPER_ADMIN | Role을 STAFF ↔ BRANCH_ADMIN으로 전환(`@IsIn`으로 화이트리스트 검증) |

### A-4. 권한/보안 규칙 매트릭스
| API 그룹 | SUPER_ADMIN | BRANCH_ADMIN | STAFF | MEMBER |
|---|---|---|---|---|
| /accounts | 전체 CRUD | 본인 지점 직원 계정만 생성/비활성화 | - | - |
| /permissions/staff (Role 전환) | 전체 조회/전환 | 접근 불가(403) | 접근 불가(403) | - |
| /branches | 전체 CRUD | 조회(본인 지점) | 조회(본인 지점) | - |
| /staff, /staff/:id | 전체 CRUD | 본인 지점 CRUD | 본인 레코드만 조회(수정은 변경 신청으로만) | - |
| /attendance, /leave-requests, /work-logs | 조회 전체 | 본인 지점 조회 + 휴가 승인 | 본인 레코드만 CRUD(휴가는 신청만) | - |
| /members, /reservations, /programs 등 | 조회 전체 | 본인 지점 CRUD | 조회 불가(403) | 본인 데이터만 |
| 모든 조회 API list 파라미터 | branchId 자유 지정 | branchId 강제 고정(본인 값) | branchId + staffId 강제 고정 | memberId 강제 고정 |

STAFF는 `BranchScopeGuard`로 지점이 고정되는 것에 더해 근태/휴가/업무일지 API에서 `staffId`까지 본인 값으로 강제된다(동료 직원 근태 열람도 차단).

### A-5. 구현 우선순위
**범위 제외(2026-09-20)**: 범용 계정 발급/목록 API(`/accounts` — 채용·회원등록이 각자 계정을 만들어 중복), 로그인 5회 실패 잠금(RFP 비요구). 퇴사·탈퇴 즉시 접근 차단은 RFP 요구라 제외 대상 아니며 이미 구현됨.
**Phase 1(완료)**: 로그인/로그아웃/토큰갱신, Role 4종 Guard, BranchScopeGuard, 계정 비활성화.
**Phase 2**: Refresh Token rotate만 완료. 로그인 잠금·이메일 비밀번호 재설정·로그인 이력 화면은 미구현.
