# 도메인 사이클 — 회원관리

> **상태**: 초안 (2026-09-22). [process/03_도메인_사이클_템플릿.md](../process/03_도메인_사이클_템플릿.md)의 11단계를 적용한 2호 사이클.
> **대상 코드**: `apps/api/src/modules/members/`
> **이 문서가 회원관리 도메인의 단일 문서다.** 옛 [1-6_회원관리.md](../1.spec/20_이용자서비스/1-6_회원관리.md)의 내용을 병합했다 — 사용자 스토리는 §0, 나머지 레퍼런스는 부록 A. 1-6문서는 **폐기 예정**(대체됨 배너 추가) — 3개 문서(`1-1_공통설계서.md`, `2-3_요구사항추적표.md`, `4-1_발표자료_핸드오프.md`(보호 영역))가 참조 중이라 링크 정리 후 삭제. 진행 상황은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md) 참고.
> **권한관리 사이클에서 이어받은 것**: AUTH-T05("탈퇴 회원 이메일 재사용")를 이 사이클에서 확정한다 — 아래 D2.
> **선행 관찰(레퍼런스 오류 발견)**: 옛 §6은 "담당 직원(assignedStaffId) 배정 시 지점 불일치를 막는 서버측 검증이 아직 미구현"이라고 적어 뒀지만, 실제 코드(`assertStaffInBranch`)는 `createMember`·`updateMember` 양쪽에서 이미 이 검증을 하고 있다. **레퍼런스가 코드보다 낡아 있던 사례** — 부록 A에서 정정했다. 이 문서가 만들어진 이유(문서·코드 괴리 추적)를 그대로 보여주는 사례라 기록해 둔다.

## 0. 이 도메인 개요 (사용자 스토리)

- 회원으로서, 앱에서 회원가입 후 내가 다니는 지점을 선택하고 이용을 시작하고 싶다.
- 지점 관리자로서, 신규 방문 회원을 현장에서 직접 등록(오프라인 등록)할 수도 있어야 한다.
- 지점 관리자로서, 회원의 수강/예약/PT 잔여 세션을 한 화면에서 확인하고 싶다.
- 회원으로서, 내 PT 잔여 세션이 몇 회 남았는지 앱에서 확인하고 싶다.

RFP 원본 요구사항(회원 인적사항·수강내역·예약및결제내역·PT잔여세션 기록, 타 현장 조회 불가)은 이 도메인이 시스템의 핵심(원본 산출물의 본질)이라 **조정 없이** 그대로 구현 대상이다 — 권한관리처럼 범위를 줄인 지점이 없다. 다만 "회원 등급제(VIP 등 마케팅 세그먼트)"처럼 원본에 없는 기능은 추가하지 않는다(범위 확장 금지, 이 원칙 자체가 §1 불변규칙은 아니지만 §8 대안 검토에서 계속 적용됨).

## 1. 요구사항 재분류

### 기능
| ID | 내용 |
|---|---|
| F05a | 각 현장별 회원 인적사항·프로그램 수강내역·예약및결제내역·PT잔여세션 기록 |
| F05b | 현장관리자는 해당 현장 회원정보만 확인(타 현장 조회 불가) |

### 품질
| ID | 내용 |
|---|---|
| Q10 | 인사·회원 등 개인정보가 별도 절차 없이 노출되지 않음(전화번호 마스킹 등) |
| Q11 | 접근 통제가 그룹별·개인별·기능별로 구체적이어야 함 |

### 제약
- C03(개인정보보호법 제26조)이 직접 제약: Member는 이 법이 다루는 개인정보 그 자체이고, 본사-지점 위탁관계 하에서 처리된다.

### 불변규칙 (협의 불가)
1. **BRANCH_ADMIN·MEMBER는 자기 소속 범위를 벗어난 회원 데이터에 접근할 수 없다.** (CLAUDE.md 4대 불변식 — 지점 데이터 격리. F05b 원문이 괄호로 강조한 그 요구사항)
2. **위탁계약이 TERMINATED된 지점에는 신규 회원을 등록할 수 없다.** (CLAUDE.md 4대 불변식 — 계약 종료 지점 차단)
3. **만 19세 미만 회원은 법정대리인 동의 없이 등록될 수 없다.** (민법상 미성년자 단독 계약 취소가능성 — RFP가 아니라 법이 정한 것이라 협의 대상이 아님)

## 2. 되묻기 확정

| ID | 질문 | 상태 | 확정 내용 |
|---|---|---|---|
| Q-08 | "타 현장 조회 불가"의 범위(본사 열람 범위, 목록·검색·상세 등 모든 경로) | **이미 확정·구현·검증됨** | SUPER_ADMIN 전체 열람 허용, BRANCH_ADMIN/MEMBER는 모든 경로(목록·상세·수정·상태변경)에서 강제 스코프. `branch-isolation.spec.ts`로 검증됨(§11) |
| **Q-26 (신규, AUTH-T05 확정)** | 탈퇴 회원과 동일 이메일 재가입을 허용할까? | **이 사이클에서 결정** | 아래 D2 참고 |
| **Q-27 (신규)** | 회원 상세 응답(`GET /:id`)에 수강·예약·PT 정보를 얼마나 포함할까? | **이 사이클에서 결정** | 아래 D3 참고 |

## 3. 수치화

| 항목 | 목표 | 근거 |
|---|---|---|
| 지점 격리 위반 | 0건 | 불변규칙 1 |
| 계약종료 지점 신규 등록 | 0건 통과 | 불변규칙 2 |
| 미성년 무동의 등록 | 0건 통과 | 불변규칙 3 |
| 연결(`/members/link`) 실패 허용 횟수 | 시간당 5회 | 권한관리 도메인의 로그인 잠금과 동일 원칙(대입 공격 지연) — RFP 비요구, 스펙이 이미 정함 |
| 전화번호 중복 | 차단 아님(경고만) | 가족 공유폰 등 현실적 케이스 — 완전 차단은 오탐 비용이 더 큼 |

**[공용 산정 상수](../architecture/design-constants.md) 재사용**: 지점당 등록 회원 550명, 3년 후 전사 최대 ≈71,500명(98→130개 업장 가정). 이 수치는 §6(Baseline)에서 회원 검색 방식의 baseline 리스크를 짚는 근거로 쓴다 — 새로 산정하지 않는다.

## 4. Architecture Driver 식별 (3개로 제한)

| # | Driver | 왜 구조를 바꾸는가 |
|---|---|---|
| D1 | 오프라인↔앱 계정 연동 인증 방식 | 인증 요소 선택이 개인정보 유출 가능성과 구현 복잡도를 동시에 결정 |
| D2 | 탈퇴 회원 이메일 재사용 정책 | Account.email 유니크 제약의 스코프(전역 vs 부분)를 결정 — 스키마 결정 |
| D3 | 회원 상세 응답의 요약 정보 범위 | API 응답 형태와 프론트 로딩 전략(1회 조회 vs 탭별 지연 로드)을 결정 |

**Driver로 승격하지 않은 것 (Tier2 — 규칙표로만 관리)**

| ID | 규칙 | 근거 | 처리 |
|---|---|---|---|
| MEM-T01 | 전화번호 중복은 경고만(차단 아님) | 이미 결정·구현됨, 구조 안 바뀜 | 구현 완료(`createMember`), 테스트 없음 |
| MEM-T02 | 담당 직원(`assignedStaffId`)은 회원과 같은 지점 소속이어야 함 | 이미 구현됨(`assertStaffInBranch`), Driver로 볼 만큼 대안이 여럿이지 않음(당연 규칙) | 구현 완료, **테스트 없음** — 옛 스펙은 "미구현"이라고 오기재돼 있었음(정정) |
| MEM-T03 | `memberNo` 형식 `{지점코드}{연도}-{순번}` | 채번 규칙, 구조 안 바뀜 | 구현 완료(`generateMemberNo`) |
| MEM-T04 | 회원 목록 검색(`q`)은 이름·회원번호·전화번호 부분일치 | 구현 디테일 | 구현 완료 |

## 5. Driver 우선순위

**불변규칙(§1) 항상 최우선** → D1(개인정보 접근 방식이라 보안 리스크 큼) → D3(RFP 핵심 스토리인 "한 화면 조회"와 직결) → D2(스키마 결정이지만 아직 해당 기능 자체가 미구현이라 리스크는 상대적으로 낮음)

## 6. 현행 구조 진단 (Baseline)

옛 §6이 이미 D1의 baseline을 진단해 뒀다 — 그대로 가져온다: 원래 Phase 1 권장안이던 "회원번호만 입력하면 연결"은 `memberNo`가 `{지점코드}{연도}-{순번}`로 **순차 생성되는 예측 가능한 값**이라는 걸 놓친 설계였다. 몇 번만 시도하면 타인의 회원번호를 맞혀 PT 세션·수강내역·연락처에 접근할 수 있다. 이게 D1을 만든 원인이다(정정된 방식은 §8에서 ADR로 재확인).

**D3의 baseline**: `MembersController.detail()`이 현재 인적사항만 반환하고, 수강내역·예약/결제내역·PT세션은 "해당 mock 엔티티가 아직 없어 후속 작업으로 남겨둔다"는 주석과 함께 비어 있다. RFP F05a("수강내역·예약및결제내역·PT잔여세션 기록")와 사용자 스토리("한 화면에서 확인")의 핵심을 아직 못 채운 상태 — 이게 이 도메인에서 가장 시급한 실제 구현 공백이다.

**D2의 baseline**: `/members/register`(앱 회원가입) 자체가 미구현이라 아직 실제로 터진 문제는 없다. 하지만 Account.email이 전역 unique이고 탈퇴 시 값 변형 없이 `isActive=false`만 하는 현재 패턴(권한관리 도메인 §10 ADR-AUTH-01·02와 동일 계열)을 그대로 두면, 나중에 `/members/register`를 구현하는 순간 "탈퇴 후 같은 이메일로 재가입 불가"라는, 누구도 요구하지 않은 제약이 생긴다.

## 7. 설계 질문 (Driver당 1개)

1. 오프라인 등록 회원이 앱 계정을 연결할 때 무엇으로 본인을 증명하게 할까?
2. 탈퇴한 회원과 같은 이메일로 재가입하면 어떻게 될까?
3. 회원 상세 화면 진입 시 API를 몇 번 불러야 할까?

## 8. 대안 → 트레이드오프 → 결정

### 질문 1: 오프라인↔앱 연동 인증 방식

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 회원번호만 입력 | 구현·UX 최단순 | **회원번호가 순차값이라 브루트포스 가능** — 개인정보 유출로 이어짐(불변규칙과는 별개지만 Q10 위반) |
| B. 회원번호+전화번호 둘 다 일치 (채택) | 전화번호는 추측 불가능한 개인정보라 브루트포스 방어, 관리자 승인 워크플로 없이 Phase 1 그대로 유지 | SMS 인증만큼의 소유권 증명은 아님(전화번호를 "안다"는 것과 "그 번호를 실제로 쓴다"는 건 다름) |
| C. SMS 인증(실제 소유권 증명) | 가장 안전 | 문자 발송 인프라·비용 필요, Phase 1 범위 초과 |

**결정**: B. 판단 기준: A는 Q10(개인정보 비노출) 위반 소지가 있어 배제, C는 인프라·일정 제약(C01)으로 Phase 2로 미룸.
**감수하는 것**: SMS만큼의 소유권 증명은 아니라서, 이미 그 사람의 전화번호를 알고 있는 지인이 회원번호까지 안다면 뚫릴 수 있다 — 시간당 5회 제한으로 대입 공격만 늦춘다.

### 질문 2: 탈퇴 회원 이메일 재사용

| 대안 | 장점 | 단점 |
|---|---|---|
| A. Account.email 전역 unique 유지(현행) | 구현 단순, 이메일=영구 고유 식별자로 감사 추적 쉬움 | 탈퇴 후 같은 이메일로 재가입이 영구히 불가 — 누구도 요구하지 않은 제약, 실사용자 불편 |
| B. 탈퇴 시 이메일 값을 변형(`withdrawn+timestamp+원본`)해 unique 제약 우회 | 재가입 허용, DB 제약 그대로 사용 가능 | 원본 이메일 조회 시 변형 로직을 항상 거쳐야 함(CS 응대 시 번거로움) |
| **C. 부분 unique 인덱스(활성 계정끼리만 유일) — 채택** | 이메일 값 원본 그대로 보존(감사·CS 유리), 재가입 시 완전히 새 Account 생성 | MockDataService(배열)에는 부분 unique 인덱스 개념이 없어 앱 레벨에서 "isActive인 계정끼리만 검사"를 직접 구현해야 함(실DB 전환 시 진짜 부분 인덱스로 교체) |

**결정**: C. 판단 기준: 이 프로젝트가 이미 예약 중복 방지(1-7문서)에서 쓴 "부분 unique 인덱스" 패턴과 원칙을 통일 — 같은 문제(활성 상태만 유일성 강제)를 도메인마다 다른 방식으로 풀지 않는다.
**감수하는 것**: 아직 `/members/register`가 미구현이라 이 결정은 코드로 확인되지 않은 가설이다. 구현 시 이 ADR대로 만든다.
**권한관리 도메인 갱신 필요**: [domains/권한관리.md](./권한관리.md) 부록 A-1의 Account 데이터모델과 AUTH-T05 항목이 이 결정을 반영하도록 갱신해야 한다(§12에서 처리).

### 질문 3: 회원 상세 응답 범위

| 대안 | 장점 | 단점 |
|---|---|---|
| A. `GET /:id`가 수강+예약+PT를 전부 조인해서 반환 | 프론트 API 호출 1번, 스토리("한 화면") 문구와 가장 직접적으로 일치 | 상세 진입마다 무거운 조인, members 컨트롤러가 다른 도메인(reservations·payments) mock 데이터에 의존하게 됨(실DB 전환 시 서비스 간 호출로 리팩터링 필요) |
| **B. `GET /:id`는 요약 카운트만(수강중 N건·PT 잔여 합계·최근 결제일), 탭 클릭 시 `/:id/enrollments` 등 개별 API로 지연 로드 — 채택** | 이미 §5 API 명세가 이 하이브리드 형태로 설계돼 있었음(상세="요약 포함", 탭별=별도 API), 상세 진입이 가볍고 탭은 필요할 때만 로드 | 요약 카운트 계산 로직과 탭 API 로직이 일부 중복 |
| C. 요약 없이 탭 API만(상세는 인적사항만) | 가장 단순 | 스토리("PT 잔여 세션이 몇 회 남았는지 확인")를 상세 진입 즉시 못 보여줌 — 탭을 눌러야 앎 |

**결정**: B. 판단 기준: 이미 §5(API 명세)가 이 형태로 설계돼 있었다 — 새로 만드는 게 아니라 **설계된 대로 구현이 안 된 공백을 메우는 것**. C는 사용자 스토리를 약화시켜 배제.
**감수하는 것**: 요약 카운트(예: `enrollmentCount`, `ptRemainingTotal`, `lastPaymentAt`)를 계산하는 로직이 각 탭 API의 카운트 로직과 별개로 한 번 더 필요하다 — 중복이지만, 상세 진입 성능과 맞바꾼다.

## 9. 감수하는 것 (종합)

- D1: SMS 수준의 소유권 증명은 아니다 — Phase 2로 이월.
- D2: 아직 코드로 검증되지 않은 가설이다 — `/members/register` 구현 시점에 실제로 이 ADR대로 만들어야 한다.
- D3: 요약 카운트 로직 중복 — 성능과 맞바꾼 선택.
- **이 사이클에서 발견한 가장 큰 공백은 D3 자체가 아니라, RFP 핵심 요구(F05a)의 절반이 아직 코드에 없다는 사실이다.** Driver 결정은 끝났지만 구현은 남아 있다(§11).

## 10. ADR

### ADR-MEM-01: 오프라인↔앱 연동 — 회원번호+전화번호 동시 일치
- **컨텍스트**: 회원번호 단독 연결은 순차값이라 브루트포스 가능(§6 baseline)
- **검토한 대안**: A 회원번호만 / B 회원번호+전화번호 / C SMS 인증
- **결정**: B. A는 Q10 위반 소지, C는 일정·인프라 제약
- **결과**: 좋아진 것 — 대입 공격 방어, Phase 1 범위 유지 / 감수하는 것 — 소유권 완전 증명은 아님 / 숙제 — Phase 2 SMS 인증
- **구현(2026-09-26)**: `POST /members/link`(공개 라우트). `mock-data.service.ts`의 `linkMemberAccount()`가 memberNo+phone 동시 일치를 확인하고, 불일치 사유(회원번호 오류 vs 전화번호 오류)를 구분하지 않고 동일하게 `MEMBER_LINK_MISMATCH`(400)로 응답한다 — 부분 정보를 공격자에게 주지 않기 위함. 실패 횟수는 memberNo 기준으로 추적(공격 대상 회원번호별 1시간 슬라이딩 윈도, 5회 초과 시 429 `LINK_ATTEMPTS_EXCEEDED`). WITHDRAWN 회원은 연동 대상에서 제외(대조군: DORMANT는 허용). 연동 성공 시 `AuthService.issueSession()`(로그인과 동일한 토큰 발급 경로를 재사용)으로 즉시 로그인시킨다. **부수 발견**: `BranchScopeGuard`가 `@Public()` 메타데이터를 확인하지 않아 인증 없는 요청을 전부 403으로 막고 있었다 — `JwtAuthGuard`와 동일하게 `Reflector`로 `IS_PUBLIC_KEY`를 확인하도록 고쳤다(D2도 같은 문제를 겪었을 것이라 미리 공통 해결됨).

### ADR-MEM-02: 탈퇴 회원 이메일 — 부분 unique(활성 계정끼리만)
- **컨텍스트**: Account.email 전역 unique를 그대로 두면 탈퇴 후 재가입이 영구 불가(§6 baseline, AUTH-T05 이월)
- **검토한 대안**: A 전역 unique 유지 / B 탈퇴 시 이메일 값 변형 / C 부분 unique 인덱스
- **결정**: C. 1-7문서(예약 중복방지)와 같은 패턴 원칙 통일
- **결과**: 좋아진 것 — 원본 이메일 보존, 도메인 간 설계 일관성 / 감수하는 것 — MockDataService에서는 앱레벨 구현 필요 / 숙제 — `/members/register` 구현 시 이 ADR대로
- **구현(2026-09-26)**: `POST /members/register`(공개 라우트). `mock-data.service.ts`에 `isEmailTakenByActiveAccount()`(이메일 유일성을 `isActive !== false`인 계정끼리만 검사)를 신설해 회원가입 시 사용했고, D1에서 이미 구현한 `linkMemberAccount()`의 이메일 중복 검사도 같은 메서드로 바꿔 두 기능이 일관되게 동작하도록 했다(§8 "같은 문제를 도메인마다 다르게 풀지 않는다" 원칙을 도메인 내부에서도 그대로 적용). `registerMember()`는 이메일 중복 검사를 먼저 하고 나서 기존 `createMember()`(계약종료 409·미성년 무동의 400·전화번호 중복 경고를 이미 갖고 있음)를 그대로 호출한다 — 인메모리 배열이라 트랜잭션이 없으므로 "실패 가능성이 있는 검사를 먼저 해서 부분 생성을 막는" 순서로 롤백을 대신했다. 가입 성공 시 D1과 동일하게 `AuthService.issueSession()`으로 즉시 로그인시킨다.

### ADR-MEM-03: 회원 상세 응답 — 요약 카운트 + 탭별 지연 로드
- **컨텍스트**: `GET /:id`가 현재 인적사항만 반환, F05a·사용자 스토리 미충족(§6 baseline)
- **검토한 대안**: A 전체 조인 / B 요약+탭별 지연 로드 / C 요약 없음
- **결정**: B. 기존 API 명세(§5)가 이미 이 형태로 설계돼 있었음
- **결과**: 좋아진 것 — 상세 진입 가벼움, 스토리 충족 / 감수하는 것 — 카운트 로직 중복 / 숙제 — 없음
- **구현(2026-09-26)**: `mock-data.service.ts`에 `memberSummary()`(enrollmentCount·ptRemainingTotal·lastPaymentAt 집계) + `courseEnrollments`/`ptSessions`/`ptSessionLogs` 배열 신설. `createEnrollment`/`createPTSession`은 `assertProgramInBranch`(프로그램-회원 지점 불일치 400)와 계약종료 지점 차단(409, 불변규칙 2)을 통과해야 하고, `usePTSession`(세션 차감)은 "신규 활동"이 아니라 TERMINATED 지점에서도 허용(ADR-FAC-03과 동일한 신규활동 vs 정정 구분). 예약·결제 내역 탭은 새 엔드포인트 대신 기존 `GET /reservations`에 `?memberId=` 필터만 추가해 재사용(`ReservationRow`가 이미 `payment`를 중첩).

## 11. 검증 현황 (정직하게 보고)

| 항목 | 상태 |
|---|---|
| BRANCH_ADMIN 타 지점 회원 상세/수정/상태변경 시 403 (불변규칙 1) | **검증됨** — `branch-isolation.spec.ts` |
| MEMBER 본인이 타 지점 회원 조회/수정 시 403 (불변규칙 1) | **검증됨** — 동일 파일 |
| SUPER_ADMIN 전체 조회 가능(대조군) | **검증됨** — 동일 파일 |
| 계약종료 지점 신규 회원 등록 409 (불변규칙 2) | **검증됨** — `contract-termination.spec.ts` |
| 미성년 무동의 등록 400 (불변규칙 3) | **검증됨(2026-09-22)** — `apps/api/test/members-validation.spec.ts` |
| 담당 직원 지점 불일치 400 (MEM-T02) | **검증됨(2026-09-22)** — 동일 파일 |
| 전화번호 중복 경고(차단 아님) | **검증됨(2026-09-22)** — 동일 파일 |
| D1(회원번호+전화번호 동시 일치, 시간당 5회 제한) | **검증됨(2026-09-26)** — `apps/api/test/member-link.spec.ts`(10건: 성공+즉시로그인, 회원번호/전화번호 불일치 동일취급, 이미연동 409, 이메일중복 409, WITHDRAWN 차단+DORMANT 허용 대조군, 5회 초과 429+대상 회원번호별 독립 대조군) |
| D2(탈퇴 회원 이메일 재사용) | **검증됨(2026-09-26)** — `apps/api/test/member-register.spec.ts`(9건: 가입+즉시로그인, 계약종료 409, 미성년 무동의 400+동의 시 대조군, 활성계정 이메일중복 409, **탈퇴 회원과 동일 이메일로 재가입 성공(ADR-MEM-02 핵심)**, 이메일 원본 보존 확인, 실패 시 Member 미생성, 존재하지 않는 지점 404) |
| D3(요약+탭별 지연 로드) | **검증됨(2026-09-26)** — `apps/api/test/member-detail-summary.spec.ts`(14건: 요약 필드, 수강내역 GET/POST+지점불일치 400+계약종료 409+타지점 403, PT세션 GET/POST/사용+소진 409+계약종료 시 신규구매만 차단+타지점 403, 예약·결제 내역 스코프). admin-web `MemberDetailPage.tsx` 3개 탭(수강내역/PT잔여세션/예약결제) Playwright로 실제 렌더링·클릭 확인 완료. |

**다음 행동 제안(우선순위순) — 5 완료(2026-09-26)**:
1. ~~D3 구현(회원 상세에 요약 필드 추가)~~ → 완료. `CourseEnrollment`/`PTSession`/`PTSessionLog` mock 모델 신설, `GET /members/:id` 요약 필드, `enrollments`/`pt-sessions`/`pt-sessions/:id/use` 엔드포인트, `GET /reservations?memberId=` 필터 추가, admin-web 3개 탭 구현.
2. ~~MEM-T02·불변규칙 3 테스트 추가~~ → `members-validation.spec.ts`로 완료(전화번호 경고 포함 보너스 커버리지).
3. ~~D1(`/members/link`) 구현~~ → 완료.
4. ~~D2(`/members/register`) 구현~~ → 완료.
5. **다음 작업**: AUTH-T05 문서 동기화(§12) — 권한관리 문서에 이 도메인의 ADR-MEM-02 결정을 반영.

## 12. 이월 사항 + 문서 마이그레이션 메모

- **AUTH-T05 해소**: [domains/권한관리.md](./권한관리.md)의 §4 Tier2 표 AUTH-T05 항목과 부록 A-1(Account 데이터모델)을 ADR-MEM-02(부분 unique) 반영해 갱신해야 한다 — 다음 작업으로 남긴다(이 문서 하나만으로는 권한관리 문서를 고치지 않음, 별도 승인 후 처리).
- D1·D2·D3 모두 "결정은 났지만 구현은 없음" 상태다. 도메인 문서 작업과 실제 구현 작업의 순서를 어떻게 가져갈지(9개 도메인 문서를 다 만든 뒤 구현 vs 도메인마다 바로 구현) 사용자 확인이 필요하다.
- 옛 스펙 문서와의 겹침 판정은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md)에 기록했다(이 문서에서 실행하지 않음).

---

## 부록 A. 레퍼런스 스펙 (결정을 반영한 현재 상태)

> §1~12(분석·결정)의 **결과물**이다. 옛 [1-6_회원관리.md](../1.spec/20_이용자서비스/1-6_회원관리.md)에서 병합했다. 여기 내용이 위 결정과 어긋나면 여기를 고친다.

### A-1. 데이터 모델

**Member**
| 필드 | 설명 |
|---|---|
| id | UUID |
| accountId | FK → Account, nullable(오프라인 등록 시 앱 미가입 상태로 먼저 생성 가능 → 이후 연결) |
| branchId | FK → Branch(주 소속 지점) — `Branch`는 위탁계약 현장, 계약 필드는 [1-1문서 §2-1] 참고 |
| assignedStaffId | FK → Staff, nullable — 담당 직원. 없으면 "회원권만(미배정)" |
| memberNo | string, unique — `{지점코드}{연도}-{순번}` |
| name, phone, birthDate, gender | |
| joinedAt | date |
| status | enum(ACTIVE, DORMANT, WITHDRAWN) |
| guardianConsent | boolean, default false |
| memo | text, nullable |

**CourseEnrollment**(수강내역, 구현됨): `id, memberId(FK), programId(FK), enrolledAt, expiresAt, status`
**PTSession**(PT 잔여세션, 구현됨): `id, memberId, programId, totalSessions, usedSessions, remainingSessions(computed), purchasedAt` + `PTSessionLog(id, ptSessionId, usedAt, note)`

Reservation·Payment는 1-7문서(→ 향후 `domains/예약및결제.md`)에서 정의하며, 회원 상세는 이들을 memberId로 조회해 통합 표시한다(ADR-MEM-03).

인덱스: `Member.branchId`, `Member.memberNo` unique, `Member.phone`(중복 확인용), `Member.assignedStaffId`

### A-2. 화면 목록
| 화면 | 대상 | 설명 |
|---|---|---|
| 회원 목록 | BRANCH_ADMIN(본인 지점), SUPER_ADMIN(전체+필터) | 검색(이름/전화번호/회원번호), 상태 필터, 담당 직원 컬럼 |
| 지점 상세(드릴다운) | SUPER_ADMIN | 지점 → 직원(담당 회원) → 미배정 회원 → 프로그램 순 |
| 회원 상세 | BRANCH_ADMIN | 인적사항 + 수강내역·예약/결제·PT잔여세션 탭(ADR-MEM-03) — **구현·화면 완료(2026-09-26)** |
| 회원 등록(현장 등록) | BRANCH_ADMIN | 오프라인 신규회원 등록 — **구현·화면 완료(2026-09-18 이후 커밋 cb2540a)** |
| 회원가입 | Member(앱) | 지점 선택 → 정보 입력 → 가입 — **미구현** |
| 내 정보 | Member(앱) | 인적사항 조회/수정, PT 잔여세션 — **미구현** |

### A-3. API 명세
| Method | Endpoint | 권한 | 설명 | 구현 상태 |
|---|---|---|---|---|
| POST | /api/v1/members/register | 공개 | Account+Member 동시 생성(ADR-MEM-02 부분 unique 이메일) | 구현됨 |
| POST | /api/v1/members/link | 공개 | ADR-MEM-01(회원번호+전화번호, 시간당 5회 제한) | 구현됨 |
| POST | /api/v1/members | BRANCH_ADMIN | 현장 오프라인 등록 | 구현됨 |
| GET | /api/v1/members?branchId=&status=&q= | SUPER_ADMIN/BRANCH_ADMIN | 목록 검색 | 구현됨 |
| GET | /api/v1/members/:id | BRANCH_ADMIN/SUPER_ADMIN/MEMBER(본인) | 상세(인적사항 + ADR-MEM-03 요약 필드) | 구현됨 |
| PATCH | /api/v1/members/:id | BRANCH_ADMIN/MEMBER(본인 일부) | 수정 | 구현됨 |
| PATCH | /api/v1/members/:id/status | BRANCH_ADMIN | 상태 전환 | 구현됨 |
| GET | /api/v1/members/:id/enrollments | 권한자 | 수강내역 | 구현됨 |
| POST | /api/v1/members/:id/enrollments | BRANCH_ADMIN | 수강 등록(계약종료 지점 409, 타지점 프로그램 400) | 구현됨 |
| GET | /api/v1/members/:id/pt-sessions | 권한자 | PT 세션 현황(잔여·사용로그) | 구현됨 |
| POST | /api/v1/members/:id/pt-sessions | BRANCH_ADMIN | PT 패키지 등록(계약종료 지점 409) | 구현됨 |
| POST | /api/v1/members/:id/pt-sessions/:sessionId/use | BRANCH_ADMIN | 세션 차감(계약종료와 무관, 소진 시 409) | 구현됨 |
| GET | /api/v1/reservations?memberId= | BRANCH_ADMIN(본인 지점)/SUPER_ADMIN | 예약·결제 내역(ADR-MEM-03 탭, 기존 엔드포인트 재사용) | 구현됨 |

### A-4. 권한 요약
- BRANCH_ADMIN: 본인 지점 회원 CRUD, 타 지점 불가(하드 룰)
- SUPER_ADMIN: 전체 조회, 개별 수정은 기술적으로 허용(예외 상황 대응)
- MEMBER 본인: 자신의 레코드만 조회/일부 수정(`assignedStaffId`·`memo`는 관리자 전용, 컨트롤러에서 차단)
- STAFF: 회원 관리 API 접근 불가(403)

### A-5. 구현 우선순위
**Phase 1(완료, API+화면)**: 회원 CRUD(현장 등록/조회/수정/상태전환), 지점별 격리, 미성년 동의 검증, 회원 상세 요약+수강내역·PT잔여세션·예약결제 탭(ADR-MEM-03, 2026-09-26), 오프라인↔앱 연동(ADR-MEM-01, `/members/link`, 2026-09-26), 앱 회원가입(ADR-MEM-02, `/members/register`, 2026-09-26).
**Phase 1(미완)**: 없음 — D1·D2·D3 전부 구현 완료. §12의 AUTH-T05 문서 동기화만 남음.
**Phase 2(선택)**: 오프라인↔앱 연동 승인 플로우, 회원 상태 자동 휴면 전환(6개월 미방문 배치), 전화번호 마스킹 UI.
