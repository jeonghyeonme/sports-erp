# 도메인 사이클 — 인사정보관리

> **상태**: 초안 (2026-09-22). [process/03_도메인_사이클_템플릿.md](../process/03_도메인_사이클_템플릿.md)의 11단계를 적용한 3호 사이클.
> **대상 코드**: `apps/api/src/modules/staff/`
> **이 문서가 인사정보관리 도메인의 단일 문서다.** 옛 [1-3_인사정보관리.md](../1.spec/10_인사조직/1-3_인사정보관리.md)의 내용을 병합했다 — 사용자 스토리는 §0, 나머지 레퍼런스는 부록 A. 1-3문서는 **폐기 예정**(대체됨 배너 추가) — 6개 문서가 참조 중이라 링크 정리 후 삭제. 진행 상황은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md) 참고.
> **선행 관찰(코드 주석 오류 발견)**: `mock-data.service.ts`의 `assignStaff` 주석은 "이미 로그인된 세션은 재로그인해야 새 branchId가 반영된다"고 적혀 있지만, 실제 `JwtStrategy.validate()`는 매 요청마다 `Account`를 재조회해 `toRequestUser(account)`로 `branchId`를 다시 구성한다(권한관리 도메인 ADR-AUTH-01과 동일 메커니즘) — `assignStaff`가 `account.branchId`도 함께 갱신하므로 실제로는 **재로그인 없이 즉시 반영된다.** 문서뿐 아니라 **코드 주석도** 코드 자체보다 낡을 수 있다는 사례 — 부록 A에서 정정했다.

## 0. 이 도메인 개요 (사용자 스토리)

- 본사 관리자로서, 신규 채용한 직원을 특정 지점에 배치(최초 파견)하고 싶다.
- 본사 관리자로서, 직원을 다른 지점으로 재배치(파견 전환)하고 그 이력을 남기고 싶다.
- 지점 관리자로서, 현재 우리 지점에 파견된 직원들의 직급/연락처 등 일상적인 인사정보를 관리하고 싶다.
- 본사 관리자로서, 전 지점 직원 현황을 하나의 화면에서 조회하고 싶다(원본 "한 화면 조회" 요건).
- 직원으로서, 내 인적사항 변경(연락처 등)을 신청하면 관리자 승인 후 반영되길 원한다.
- 지점 관리자로서, 퇴사 처리 시 퇴사일을 기록하고 계정을 자동으로 비활성화하고 싶다.

RFP 원문("행정 및 트레이너들 역시 해당 지역 또는 본사 파견 형태로 직원이 투입")이 이 도메인의 핵심 데이터 모델을 직접 결정한다 — `Staff.branchId`는 고정 소속이 아니라 **현재 파견 지점**이고, 파견 이력은 `StaffAssignment`로 별도 관리한다(CLAUDE.md "Staff는 지점 소속이 아니라 본사 소속 파견 인력" 재해석과 동일 원천).

## 1. 요구사항 재분류

### 기능
| ID | 내용 |
|---|---|
| F02a | 현장(지점)별 인사관리 탭, 코드 생성 |
| F02b | 모든 인사기록 정보를 한 화면에서 조회 |
| F02c | 권한 부여 혹은 승인 시 수정 가능 |

### 품질
- (권한관리 도메인의 Q11(접근통제)이 이 도메인에도 적용되지만, 도메인 고유 품질 요구는 RFP에 별도로 없다.)

### 제약
- C01(과업 기간)이 간접 제약: "승인 기반 수정"의 다단계 결재라인은 1인 개발 범위를 넘어 본인 신청→1단계 승인으로 단순화(아래 §8 참고).

### 불변규칙 (협의 불가)
1. **채용·재배치는 본사(SUPER_ADMIN)만 한다.** (CLAUDE.md 4대 불변식 — 인사 권한 분리. 권한관리 도메인의 불변규칙 2와 같은 원천, 이 도메인은 그 실제 구현 지점이다)
2. **한 직원은 활성(`endDate=null`) `StaffAssignment`가 항상 최대 1건이어야 한다.** (파견이 이 도메인의 핵심 모델인 이상, 한 사람이 동시에 두 지점에 파견될 수는 없다는 건 협의 대상이 아니다)
3. **퇴사 처리는 계정 비활성화와 분리될 수 없다.** (권한관리 도메인 불변규칙 1 "퇴사 즉시 접근 차단"의 트리거가 바로 이 도메인의 `/resign`이다 — 두 도메인이 공유하는 불변식의 서로 다른 쪽 절반)

## 2. 되묻기 확정

| ID | 질문 | 상태 | 확정 내용 |
|---|---|---|---|
| Q-19 | 인사관리 "코드 생성"이 무슨 코드인가 | **이미 확정·구현됨** | `staffCode`(`{최초배치지점코드}-{순번}`), 파견 전환돼도 재발급 안 함 |
| Q-20 | "승인 시 수정"의 승인 주체·워크플로 | **정정 필요 — 01번 문서 갱신 대상** | 01번 문서는 "본사 승인"으로 가정했지만, 실제 1-3문서 §1은 **BRANCH_ADMIN 1단계 승인**으로 조정해 뒀다(다단계 결재는 범위 초과). 본사가 아니라 현재 파견 지점 관리자가 승인 주체 — **단, 이 워크플로 자체가 코드에 전혀 구현되지 않음**(§6) |

## 3. 수치화

| 항목 | 목표 | 근거 |
|---|---|---|
| 파견 발령 후 접근권 전환 지연 | 0초(재로그인 불필요) | 불변규칙 1, 권한관리 ADR-AUTH-01과 동일 메커니즘 재사용 |
| 퇴사 후 접근 차단 지연 | 0초 | 불변규칙 3 |
| 활성 파견 중복 | 0건 | 불변규칙 2 |
| staffCode 중복 발급 | 0건 | D2(아래) |

**[공용 산정 상수](../architecture/design-constants.md) 재사용**: 이 도메인은 회원(지점당 550명)보다 훨씬 작은 규모(지점당 직원 수는 보통 한 자릿수~10명대)라 부하 관점의 Driver는 없다. 정확성(동시 채용 시 채번 충돌) 관점의 Driver만 유효하다 — 아래 D2.

## 4. Architecture Driver 식별 (3개로 제한)

| # | Driver | 왜 구조를 바꾸는가 |
|---|---|---|
| D1 | 퇴사/파견발령 처리의 원자성 경계 | 여러 레코드(Staff·Account·StaffAssignment)를 한 번에 바꾸는 지점 — 실DB 전환 시 트랜잭션 범위를 결정 |
| D2 | staffCode 채번 방식 | 동시 채용 시 중복 채번 가능성 — 예약및결제 도메인의 "정원 초과 방지"와 완전히 같은 문제 패턴 |
| D3 | 활성 파견 유일성 강제 메커니즘 | 애플리케이션 검사 vs DB 제약 — 이미 이 프로젝트가 두 번(예약 중복방지, 회원 이메일 재사용) 쓴 패턴의 세 번째 적용 사례 |

**Driver로 승격하지 않은 것 (Tier2 — 규칙표로만 관리)**

| ID | 규칙 | 근거 | 처리 |
|---|---|---|---|
| STF-T01 | 퇴사한 직원은 목록에서 기본 숨김, 필터로 조회 가능 | 구현 디테일 | 구현 완료 |
| STF-T02 | `PATCH /staff/:id`로는 `branchId` 변경 불가(파견 API 전용) | DTO에 필드 자체가 없어 원천 차단, 이미 구조적으로 강제됨 | 구현 완료 |
| STF-T03 | 연락처 목록 마스킹, 상세 전체노출 | UI 디테일, Phase 2 | **미구현** |
| STF-T04 | `offDays`(정기 휴무 요일) 필드 | 근태관리 도메인이 쓰는 필드인데 `MockStaff` 타입 자체에 없었음 | **구현됨(2026-09-23, 근태관리 ADR-ATT-02 작업 중 함께 처리)** — `MockStaff.offDays`, `CreateStaffDto`/`UpdateStaffDto`에 검증(0~6 정수 배열) 추가. 파트타임은 저장 시 자동으로 `undefined` 처리 |

## 5. Driver 우선순위

**불변규칙(§1) 항상 최우선** → D3(불변규칙 2를 직접 강제) → D1(불변규칙 1·3의 원자성 보장) → D2(정확성 문제지만 영향 범위가 채번 하나로 국소적)

## 6. 현행 구조 진단 (Baseline)

**D1**: `resignStaff`·`assignStaff` 둘 다 코드 주석이 스스로 인정하듯 "실DB 전환 시 여기가 트랜잭션으로 묶여야 할 지점"이다. 지금은 `MockDataService`가 단일 Node 프로세스에서 동기 실행되는 배열이라 중간 실패가 없지만, 실DB에서는 `Staff.status` 갱신 후 `Account.isActive` 갱신 전에 실패하면 "퇴사 처리됐는데 로그인은 여전히 된다"는 불변규칙 3 위반 상태가 남는다.

**D2**: `generateStaffCode`는 `this.staff.filter(...).length + 1`로 카운트한다 — **이건 1-7문서가 이미 "정원 초과 방지" §6에서 논파한 바로 그 결함 패턴**이다(팬텀 삽입: 두 채용 요청이 동시에 카운트를 읽으면 같은 순번을 계산해 같은 `staffCode`를 발급할 수 있다). 지금은 단일 프로세스라 실제로 터지지 않지만, 실DB+다중 인스턴스에서는 1-7문서와 같은 이유로 뚫린다.

**D3**: 활성 파견 최대 1건이라는 불변식은 현재 "`endDate=null`인 레코드를 찾아서 먼저 마감" 로직으로만 지켜진다. DB 제약이 아니라 애플리케이션 로직이 유일한 방어선이다.

## 7. 설계 질문 (Driver당 1개)

1. 퇴사·파견발령처럼 여러 레코드를 한 번에 바꾸는 처리를 실DB에서 어떻게 원자적으로 만들까?
2. 동시 채용 요청이 몰려도 `staffCode`가 겹치지 않으려면 어떻게 채번할까?
3. 활성 파견 최대 1건이라는 불변식을 무엇으로 강제할까?

## 8. 대안 → 트레이드오프 → 결정

### 질문 1: 트랜잭션 경계

| 대안 | 장점 | 단점 |
|---|---|---|
| **A. 하나의 DB 트랜잭션으로 Staff/Account/StaffAssignment 갱신을 묶는다 (채택)** | 표준적인 방법, 실패 시 전체 롤백으로 불변규칙 3 위반 상태가 절대 남지 않음 | 실DB 전환 전까지는 적용할 대상(트랜잭션)이 없음 — 지금은 문서화만 가능 |
| B. 순차 mutation 유지 + 실패 시 보상 로직(SAGA) | 마이크로서비스로 쪼개져도 동작 | 이 프로젝트는 단일 NestJS 모놀리스라 오버엔지니어링 — 트랜잭션 하나로 충분한 문제에 분산 트랜잭션 패턴을 쓸 이유가 없음 |

**결정**: A. 판단 기준: 단일 DB·단일 서비스 안에서의 다중 레코드 갱신은 표준 트랜잭션이 정답이고, B는 이 프로젝트 규모에 맞지 않음.
**감수하는 것**: 지금(Mock) 상태에서는 검증 불가능한 결정이다 — 실DB 전환 시점의 숙제로 명시적으로 이월한다.

### 질문 2: staffCode 채번

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 현재 카운트 방식 유지 | 이미 구현됨, 단순 | **동시 채용 시 중복 채번**(팬텀 삽입, D2 baseline) |
| **B. 지점별 시퀀스 테이블 + 행 락(`SELECT ... FOR UPDATE`) (채택)** | 1-7문서(정원 초과 방지)가 이미 검증한 해법과 완전히 같은 원리 재사용 — 이 프로젝트의 "카운트 대신 잠글 대상이 있는 리소스를 잠근다"는 표준 패턴의 세 번째 적용 | 시퀀스 테이블이라는 작은 새 엔티티 필요 |
| C. Postgres `SEQUENCE` 객체(지점별 또는 전역) | DB 네이티브라 락 코드 불필요 | 지점별 시퀀스는 지점 추가마다 동적 생성이 번거롭고, 전역 시퀀스는 "지점 내 001부터 시작"이라는 채번 규칙(§3)과 의미가 어긋남 |

**결정**: B. 판단 기준: 이미 검증된 같은 패턴을 재사용하는 게 새 해법(C)을 들이는 것보다 일관성 있고 위험이 적음.
**감수하는 것**: 지금(Mock)은 단일 프로세스라 A로도 실제로는 안 터진다 — 이 결정도 실DB 전환 전까지는 가설이다.

### 질문 3: 활성 파견 유일성 강제

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 애플리케이션 로직만(현행) | 이미 구현됨 | 동시 재배치 요청 2건이 겹치면 실DB에서 활성 파견이 2건 생길 수 있음 |
| **B. 부분 unique 인덱스(`StaffAssignment(staffId) WHERE endDate IS NULL`) (채택)** | 회원관리 ADR-MEM-02(이메일 재사용), 1-7문서(예약 중복방지)와 같은 패턴 — 이 프로젝트의 세 번째 부분 unique 적용 사례, 팀 전체가 "활성 상태만 유일성 강제"라는 하나의 원칙으로 일관됨 | MockDataService에는 부분 인덱스 개념이 없어 지금은 애플리케이션 로직(A)이 대리 역할을 함 |

**결정**: B. 판단 기준: 이 프로젝트가 이미 두 번 검증한 패턴과의 일관성 — 이번이 세 번째라 "표준 패턴"이라고 부를 수 있는 수준이 됐다.
**감수하는 것**: A가 현재 유일한 방어선이라는 사실은 실DB 전환 전까지 안 바뀐다.

## 9. 감수하는 것 (종합)

- D1·D2·D3 모두 **"결정은 났지만 Mock 환경에서는 검증도 실효도 없는" 가설**이다 — 권한관리 도메인 ADR-AUTH-01과 같은 처지(실DB 전환 후 숙제로 이월).
- 이번 사이클은 유독 "실DB 전환 이후에나 의미가 생기는 결정"이 몰렸다 — 이 도메인 자체가 다중 레코드 갱신·동시성 문제가 많은 영역이기 때문(파견·퇴사가 Staff/Account/StaffAssignment 셋을 동시에 건드림).

## 10. ADR

### ADR-STF-01: 퇴사/파견발령 — 단일 DB 트랜잭션
- **컨텍스트**: 여러 레코드를 순차 mutation으로 갱신 중 — 실DB 전환 시 중간 실패가 불변규칙 3 위반 상태를 남길 수 있음(§6)
- **검토한 대안**: A DB 트랜잭션 / B SAGA(보상 로직)
- **결정**: A. 단일 모놀리스에 분산 트랜잭션 패턴은 과함
- **결과**: 좋아진 것 — 실패 시 항상 일관 상태 / 감수하는 것 — 지금은 검증 불가 / 숙제 — 실DB 전환 시점에 실제로 트랜잭션으로 묶기

### ADR-STF-02: staffCode 채번 — 지점별 시퀀스 테이블 + 행 락
- **컨텍스트**: 현재 카운트 방식은 1-7문서가 이미 논파한 팬텀 삽입 패턴과 동일한 결함(§6)
- **검토한 대안**: A 카운트 유지 / B 시퀀스 테이블+행 락 / C DB SEQUENCE 객체
- **결정**: B. 1-7문서와 같은 검증된 패턴 재사용
- **결과**: 좋아진 것 — 중복 채번 방지 / 감수하는 것 — 새 엔티티 필요 / 숙제 — 실DB 전환 시 적용

### ADR-STF-03: 활성 파견 유일성 — 부분 unique 인덱스
- **컨텍스트**: "활성 파견 최대 1건" 불변식이 현재 애플리케이션 로직에만 의존(§6)
- **검토한 대안**: A 애플리케이션 로직만 / B 부분 unique 인덱스
- **결정**: B. 회원관리 ADR-MEM-02, 1-7문서와 같은 원칙(이 프로젝트의 세 번째 적용)
- **결과**: 좋아진 것 — DB가 최종 방어선이 됨, 도메인 간 패턴 일관성 / 감수하는 것 — 지금은 A가 유일한 방어선 / 숙제 — 실DB 전환 시 적용

## 11. 검증 현황 (정직하게 보고)

| 항목 | 상태 |
|---|---|
| 채용·재배치는 본사만 (불변규칙 1) | **검증됨** — `hr-authority.spec.ts` |
| BRANCH_ADMIN 타 지점 직원 조회/수정/퇴사 시 403 | **검증됨** — `branch-isolation.spec.ts` |
| 파견 발령 후 예전 지점 관리자 접근 상실 / 새 지점 관리자 접근 획득 | **검증됨(2026-09-22)** — `apps/api/test/staff-assignment.spec.ts` |
| 파견 발령 후 재로그인 없이 즉시 반영(§0 선행 관찰) | **검증됨(2026-09-22)** — 동일 파일(같은 테스트가 증명) |
| 퇴사 처리 후 Account.isActive=false 실제 반영 | **검증됨(2026-09-22)** — 동일 파일(재로그인 시도 401 ACCOUNT_INACTIVE) |
| staffCode 중복 없음(D2) | **검증 불가**(MockDataService 단일 프로세스라 동시성 자체가 재현 안 됨) — 실DB 전환 후 이월 |
| 활성 파견 최대 1건(D3) | **부분 검증(2026-09-22)** — 순차 시나리오(파견→재파견 후 활성 1건 유지, 퇴사 시 활성 0건)는 `staff-assignment.spec.ts`로 검증. 동시 요청 경합은 D2와 같은 이유로 검증 불가 |
| 변경 신청/승인 워크플로(Q-20) | **미구현 — 검증 대상 아님** |
| offDays(STF-T04) | **구현됨(2026-09-23)** — 자동 테스트는 근태관리 쪽(`attendance-absence.spec.ts`)이 간접 커버(offDays=[]로 고정한 시나리오만). 필드 자체의 DTO 검증(0~6 범위, 파트타임 제외) 전용 테스트는 없음 |

**다음 행동 제안 — 1·2 완료(2026-09-22)**:
1. ~~파견 발령 후 "예전 관리자 상실/새 관리자 획득 + 재로그인 없이 즉시 반영"~~ → `staff-assignment.spec.ts`로 완료.
2. ~~퇴사 처리 후 계정 비활성화 실제 검증~~ → 동일 파일로 완료.
3. D1·D2·D3는 실DB 전환 전까지 착수 범위 밖.

## 12. 이월 사항 + 문서 마이그레이션 메모

- ~~**STF-T04(offDays)**~~: 근태관리 ADR-ATT-02 구현 중 처리 완료(2026-09-23) — 아래 데이터 모델에 반영.
- **Q-20 정정**: [process/01_RFP_분류와_되묻기.md](../process/01_RFP_분류와_되묻기.md)의 Q-20 항목("본사 승인 필요"라는 임시 가정)을 "BRANCH_ADMIN 1단계 승인(미구현)"으로 갱신해야 한다 — 별도 작업으로 처리.
- 옛 스펙 문서와의 겹침 판정은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md)에 기록했다(이 문서에서 실행하지 않음).

---

## 부록 A. 레퍼런스 스펙 (결정을 반영한 현재 상태)

> §1~12(분석·결정)의 **결과물**이다. 옛 [1-3_인사정보관리.md](../1.spec/10_인사조직/1-3_인사정보관리.md)에서 병합했다.

### A-1. 데이터 모델

**Staff**: `id, accountId(FK→Account, 1:1), branchId(FK→Branch, 현재 파견 지점), staffCode(unique, {최초배치지점코드}-{순번}), name, phone, position(팀장/지점장/트레이너/행정직원/기타), employmentType(정규직/계약직/파트타임), offDays(int[], 0=일~6=토, 파트타임은 항상 undefined), hireDate, resignDate(nullable), status(ACTIVE/휴직/RESIGNED), createdAt/updatedAt`

**StaffAssignment**(파견 이력): `id, staffId(FK), branchId(FK), startDate, endDate(nullable — null이면 현재 진행 중), assignedBy(FK→Account, 항상 SUPER_ADMIN), note`. 인덱스: `(staffId, endDate)`, `(branchId, startDate)`.

**StaffProfileChangeRequest**(변경 신청, **미구현**): `id, staffId, field, oldValue, newValue, status(대기/승인/반려), reviewedBy, reviewedAt`

### A-2. 화면 목록
| 화면 | 대상 | 상태 |
|---|---|---|
| 직원 목록(지점별) | BRANCH_ADMIN | 구현됨 |
| 전체 직원 통합 조회 | SUPER_ADMIN | 구현됨 |
| 직원 신규 등록·파견 배치 | SUPER_ADMIN | 구현됨 |
| 파견 발령(재배치) | SUPER_ADMIN | 구현됨 |
| 직원 상세/수정(파견 이력 탭 포함) | BRANCH_ADMIN | 구현됨 |
| 변경 신청함 | BRANCH_ADMIN | **미구현** |
| 내 정보(변경 신청) | STAFF 본인 | 조회만 구현(`GET /staff/me`), 신청 기능 **미구현** |

### A-3. API 명세
| Method | Endpoint | 권한 | 구현 상태 |
|---|---|---|---|
| GET | /api/v1/staff | SUPER_ADMIN/BRANCH_ADMIN | 구현됨 |
| GET | /api/v1/staff/me | STAFF 본인 | 구현됨 |
| POST | /api/v1/staff | SUPER_ADMIN | 구현됨 |
| GET | /api/v1/staff/:id | 해당 지점 권한자 | 구현됨 |
| PATCH | /api/v1/staff/:id | BRANCH_ADMIN(현재 파견 지점) | 구현됨 |
| PATCH | /api/v1/staff/:id/resign | BRANCH_ADMIN | 구현됨 |
| POST | /api/v1/staff/:id/assignments | SUPER_ADMIN | 구현됨 |
| GET | /api/v1/staff/:id/assignments | 해당 지점 권한자/SUPER_ADMIN | 구현됨 |
| POST | /api/v1/staff/:id/change-requests | STAFF 본인 | **미구현** |
| PATCH | /api/v1/staff/change-requests/:id/approve\|reject | BRANCH_ADMIN | **미구현** |

### A-4. 권한 요약
- BRANCH_ADMIN: 현재 파견 지점의 직원만 CRUD(퇴사 포함), 타 지점 403
- SUPER_ADMIN 전용: 신규 등록(채용), 파견 발령
- STAFF 본인: 자기 레코드 조회만(직접 수정 불가, 변경 신청 경로는 미구현)

### A-5. 구현 우선순위
**Phase 1(완료)**: 직원 CRUD, `StaffAssignment` 기반 파견 모델, 지점별 목록/필터, 퇴사 처리(+계정 비활성화 연동).
**Phase 2(미구현)**: 파견 이력 UI 고도화, 변경 신청/승인 워크플로우, 연락처 마스킹 UI, `offDays` 입력 UI(API는 구현됨, admin-web 화면은 아직 없음).
