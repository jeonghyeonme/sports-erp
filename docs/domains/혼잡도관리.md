# 도메인 사이클 — 혼잡도관리

> **상태**: 초안 (2026-09-23). [process/03_도메인_사이클_템플릿.md](../process/03_도메인_사이클_템플릿.md) 11단계 적용 — 7호 사이클(중 우선순위, 스킬 산출물).
> **대상 코드**: `apps/api/src/modules/facilities/`
> **이 문서가 혼잡도관리 도메인의 단일 문서다.** 옛 [1-9_혼잡도관리.md](../1.spec/30_운영지원/1-9_혼잡도관리.md)의 내용을 병합했다. 1-9문서는 **폐기 예정**(대체 배너 추가) — 진행 상황은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md).

## 0. 이 도메인 개요 (사용자 스토리)

- 회원으로서, 헬스장에 가기 전 앱에서 지금 얼마나 붐비는지 확인하고 싶다.
- 지점 관리자로서, 자동 계산된 혼잡도가 실제와 다르면 수동으로 보정하고 싶다(예: 특별 이벤트로 사람이 몰릴 때).
- 지점 관리자로서, 시설별 정원 대비 이용률을 설정/관리하고 싶다.

원본 요구사항(시설별 이용현황·혼잡도를 5단계로 게시, 실시간 혹은 최대 30분 이내 반영)에서 IoT 센서·CCTV 기반 인원 계측은 하드웨어가 필요해 불가능하므로, **예약/체크인 데이터 기반 자동 계산은 Phase 2로 미루고 Phase 1은 관리자 수동 보정**으로 구현한다. QR 체크인·5분 주기 자동 계산 스케줄러는 모바일 앱과 스케줄러 인프라가 없어 범위 제외됐다(CLAUDE.md).

## 1. 요구사항 재분류

### 기능
| ID | 내용 |
|---|---|
| F08a | 시설별 이용현황·혼잡도를 5개 항목으로 나눠 게시 |
| F08b | 실시간 혹은 최대 30분 이내 이용현황 반영 |

### 품질
- F08b가 사실상 품질 요구(신선도)를 겸한다 — 아래 §3에서 수치화.

### 제약
- 하드웨어(IoT 센서·CCTV) 기반 계측 불가 — 1-9문서 §1이 이미 명시.
- 스케줄러 인프라 없음(Phase 2 QR·자동계산이 이 제약으로 범위 제외됨).

### 불변규칙 (협의 불가)
1. **BRANCH_ADMIN은 자기 지점 시설만 관리한다.** (CLAUDE.md 4대 불변식 — 지점 데이터 격리)
2. **정원(`capacity`)은 0이 될 수 없다.** (0으로 나누면 계산 자체가 성립하지 않음 — 협의 대상이 아니라 수학적 제약)

## 2. 되묻기 확정

| ID | 질문 | 상태 |
|---|---|---|
| Q-04 | "실시간 혹은 최대 30분" 중 무엇이 목표인가 | **이미 확정됨** — 1-9문서가 Phase1(수동 보정)만으로 "게시" 요건 자체는 충족한다고 판단, Phase2(5분 주기 자동계산)는 30분 요건을 상회 달성하는 확장으로 설계는 됐으나 스케줄러 부재로 미구현 |
| Q-06 | "5개 항목"이 5단계인지 5개 지표인지 | **이미 확정됨** — 5단계(여유~매우 붐빔) 이용률 매핑으로 구현됨 |

## 3. 수치화

| 항목 | 목표 | 근거 |
|---|---|---|
| 지점 격리 위반 | 0건 | 불변규칙 1 |
| capacity=0 등록 통과 | 0건 | 불변규칙 2, 이미 구현·검증 가능 |
| 5단계 매핑 정확도 | 이용률 0~20/21~40/41~60/61~80/81~% → 1~5단계, 경계값 포함 정확히 일치 | 1-9문서 §4(계산, 소스=RFP 5개 항목 요건) |
| 값의 신선도(마지막 갱신 시각) | **현재 값이 언제 갱신됐는지 API 응답에서 확인 가능해야 함** | RFP F08b(측정 조건 없던 요구를 이번 사이클에서 검증 가능하게 구체화) — 아래 D1 |

부하 관점 Driver 없음(시설 수는 지점당 소수).

## 4. Architecture Driver 식별 (3개로 제한)

| # | Driver | 왜 구조를 바꾸는가 |
|---|---|---|
| D1 | 혼잡도 값의 신선도(최신성) 추적 | RFP의 "30분 이내 반영" 요건을 검증할 수 있는지 자체를 결정 — 데이터 모델에 필드가 있어야 함 |
| D2 | 시설 운영 중단(폐쇄) 표현 방법 | 삭제 API의 존재 여부·소프트 삭제 필드 필요 여부를 결정 |
| D3 | 계약종료(TERMINATED) 지점의 시설 신규 등록·혼잡도 보정 차단 여부 | CLAUDE.md 불변식(계약종료 지점 신규활동 차단)을 이 도메인에도 적용할지 — 검증 위치를 결정 |

**Driver로 승격하지 않은 것 (Tier2 — 규칙표로만 관리)**

| ID | 규칙 | 근거 | 처리 |
|---|---|---|---|
| FAC-T01 | `capacity < 1`이면 400 | 단순 입력 검증 | 구현 완료, **테스트 없음** |
| FAC-T02 | `capacity` 변경 시 `level` 즉시 재계산 | 단순 계산 로직 | 구현 완료, **테스트 없음** |
| FAC-T03 | `capacity`를 `currentCount`보다 낮게 설정해도 차단하지 않음(level은 5로 캡) | 회원관리 전화번호 중복 등과 같은 "경고보다도 안 하는" 패턴 — 대안 비교가 뻔해 Driver로 승격할 가치가 낮음 | 구현됨(암묵적), **수용한 동작으로 명시 기록** |
| FAC-T04 | QR 체크인/아웃, 5분 주기 자동계산, 미체크아웃 자동 마감 배치, 현재 이용자 목록 | 1-9문서 §9가 이미 명시적으로 범위 제외(모바일 앱·스케줄러 없음) | 미구현, 재론 대상 아님 |

## 5. Driver 우선순위

**불변규칙(§1) 항상 최우선** → D3(CLAUDE.md 핵심 불변식의 적용 범위 문제 — 이미 있는 원칙과의 일관성) → D1(RFP 명시 요건의 검증 가능성 자체를 결정) → D2(운영상 필요하지만 리스크는 상대적으로 낮음)

## 6. 현행 구조 진단 (Baseline)

**D1**: `MockFacility`는 `currentCount`/`level`만 갖고 "언제 그 값이 정해졌는지"를 기록하는 필드가 전혀 없다. 관리자가 한 달 전에 수동 보정하고 잊어버려도, 회원 화면에는 방금 갱신된 값처럼 똑같이 보인다 — RFP F08b("최대 30분 이내 반영")를 **검증할 방법 자체가 없다**(신선한 값인지 오래된 값인지 시스템도, 사용자도 구분 불가).

**D2**: 1-9문서 §3에 `Facility.isActive`가 명시돼 있지만 `MockFacility` 타입에는 없다. 컨트롤러에도 `DELETE`가 없다 — 리모델링 등으로 운영을 멈춘 시설을 표현할 방법이 없어, 계속 목록·혼잡도 조회에 노출된다.

**D3**: `createFacility`·`setManualCongestion` 어디에도 `Branch.contractStatus` 체크가 없다. 회원등록·예약생성·게시글작성에는 이미 있는 "계약종료 지점 신규활동 차단"(CLAUDE.md 4대 불변식) 패턴이 이 도메인에만 빠져 있다 — 계약이 끝난 지점에서도 새 시설을 계속 등록하고 혼잡도를 조작할 수 있다.

## 7. 설계 질문 (Driver당 1개)

1. 혼잡도 값이 언제 갱신됐는지 어떻게 알 수 있게 할까?
2. 더 이상 운영하지 않는 시설을 어떻게 표시할까?
3. 계약이 끝난 지점에서도 시설 등록·혼잡도 보정을 계속 허용해야 할까?

## 8. 대안 → 트레이드오프 → 결정

### 질문 1: 값의 신선도 추적

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 현행 유지(추적 없음) | 없음 | RFP 요건 검증 자체가 불가능 |
| **B. `MockFacility`에 `lastUpdatedAt` 추가 — 수동 보정 시 갱신, 조회 응답에 포함 (채택)** | 최소 비용으로 RFP 요건 검증 가능, 클라이언트가 "N분 전 갱신" 표시 가능 | 이력(과거 값들)은 안 남음 — "지금 값이 언제 것인가"만 알 수 있음 |
| C. 스펙대로 별도 `CongestionSnapshot` 이력 테이블 전체 구현 | 정확한 이력 추적 | 이력이 의미 있으려면 주기적 자동 기록이 전제인데 스케줄러가 없어 지금은 "수동 보정 시점만 기록되는 듬성듬성한 이력"이 됨 — Phase2와 함께 가야 실효 |

**결정**: B. 판단 기준: C는 Phase2(스케줄러) 없이는 절반만 의미 있어 지금 단계에 과함.
**감수하는 것**: 값의 변화 이력은 안 남는다 — "지금 값이 언제 갱신됐나"만 답할 수 있다.

### 질문 2: 시설 운영 중단 표현

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 현행 유지(표현 수단 없음) | 없음 | 운영 안 하는 시설도 계속 노출 |
| **B. `isActive` 필드 추가 + 비활성화 PATCH, 목록은 기본 `isActive=true`만 (채택)** | 이 프로젝트의 소프트 삭제 원칙(1-1문서)과 일치 | 없음(패턴 재사용) |
| C. 물리 삭제 | 가장 단순 | 이 프로젝트 전역 원칙(소프트 삭제)과 정면 충돌 |

**결정**: B.
**감수하는 것**: 없음.

### 질문 3: 계약종료 지점의 시설 신규활동

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 현행 유지(체크 없음) | 없음 | CLAUDE.md 핵심 불변식과 이 도메인만 어긋남 |
| **B. `createFacility`·`setManualCongestion`에 다른 도메인과 동일한 409 `BRANCH_TERMINATED` 체크 추가, `updateFacility`(기존 시설 속성 정정)는 제외 (채택)** | 기존 패턴 재사용. "신규 활동"과 "기존 데이터 정정"을 구분해 과도한 차단을 피함(정정은 조회에 준하는 관리 활동이라는 CLAUDE.md "과거 데이터 조회는 유지" 정신과 일치) | 어디까지가 "신규 활동"이고 어디까지가 "정정"인지 경계 판단이 한 번 더 필요 |
| C. 모든 쓰기(등록·수정·보정)를 다 차단 | 가장 엄격 | 기존 시설의 오타 수정 같은 정당한 관리 작업까지 막아 과도함 |

**결정**: B. 판단 기준: CLAUDE.md가 이미 "과거 데이터 조회는 유지"라는 구분선을 그어 놨고, 그 정신을 "정정 vs 신규"에도 그대로 적용.
**감수하는 것**: "생성/보정 = 신규 활동, 수정 = 정정"이라는 구분이 다른 도메인(회원·예약 등)과 완전히 같은 칼로 그어진 건 아니다 — 이 도메인 특유의 판단이라는 걸 명시해 둔다.

## 9. 감수하는 것 (종합)

- D1: 혼잡도 변화 이력은 안 남는다.
- D2: 없음.
- D3: "신규 vs 정정" 구분이 이 도메인 특유의 판단이다.

## 10. ADR

### ADR-FAC-01: 혼잡도 값에 lastUpdatedAt 추가
- **컨텍스트**: 값의 신선도를 알 방법이 없어 RFP F08b 요건 검증이 불가능(§6)
- **검토한 대안**: A 현행 / B lastUpdatedAt 추가 / C 전체 이력 테이블
- **결정**: B. C는 스케줄러 없이는 절반만 의미 있어 과함
- **결과**: 좋아진 것 — 요건 검증 가능 / 감수하는 것 — 이력 없음 / 숙제 — 없음

### ADR-FAC-02: Facility.isActive + 비활성화 API
- **컨텍스트**: 운영 중단 시설을 표현할 방법이 없음(§6)
- **검토한 대안**: A 현행 / B isActive 추가 / C 물리삭제
- **결정**: B. 소프트 삭제 원칙과 일치
- **결과**: 좋아진 것 — 운영중단 시설 목록에서 배제 가능 / 감수하는 것 — 없음 / 숙제 — 없음

### ADR-FAC-03: 계약종료 지점의 시설 신규등록·보정 차단
- **컨텍스트**: CLAUDE.md 불변식이 이 도메인에만 적용 안 돼 있음(§6)
- **검토한 대안**: A 현행 / B 생성·보정만 차단, 수정은 허용 / C 전체 차단
- **결정**: B. "신규 활동 vs 정정" 구분을 CLAUDE.md 정신대로
- **결과**: 좋아진 것 — 불변식 일관 적용 / 감수하는 것 — 이 도메인 특유의 경계 판단 / 숙제 — 없음
- **구현됨(2026-09-25)**: `MockDataService.createFacility`·`setManualCongestion`에 `Branch.contractStatus==='TERMINATED'` 체크 추가(`createMember`/`createPost`와 동일한 인라인 패턴 — 공통 가드 중앙화는 실DB 전환 이후로 미룬다는 CLAUDE.md 결정에 따름). `updateFacility`는 의도적으로 제외. `contract-termination.spec.ts`에 시설 등록·혼잡도 보정·목록 조회·기존 시설 수정(차단 제외 확인) 케이스를 추가했다.

## 11. 검증 현황 (정직하게 보고)

| 항목 | 상태 |
|---|---|
| BRANCH_ADMIN 타 지점 시설 수정/보정 시 403 (불변규칙 1) | **검증됨** — `branch-isolation.spec.ts` |
| capacity < 1 등록/수정 거부 (불변규칙 2) | **검증되지 않음** — 로직은 있으나 전용 테스트 없음 |
| 5단계 매핑 경계값 정확도(FAC-T02) | **검증되지 않음** |
| ADR-FAC-01(lastUpdatedAt) | **구현 안 됨 — 검증 대상 아님** |
| ADR-FAC-02(isActive) | **구현 안 됨 — 검증 대상 아님** |
| ADR-FAC-03(계약종료 차단) | **구현됨, 검증됨(2026-09-25)** — `contract-termination.spec.ts` |

**다음 행동 제안(우선순위순)**:
1. ~~ADR-FAC-03(계약종료 차단)~~ — 2026-09-25 구현·검증 완료.
2. capacity 검증·5단계 매핑 경계값 테스트 추가 — 이미 구현된 로직.
3. ADR-FAC-01(lastUpdatedAt) 구현.
4. ADR-FAC-02(isActive) 구현.

## 12. 이월 사항 + 문서 마이그레이션 메모

- ADR-FAC-03이 다루는 "신규 활동 vs 정정" 구분은 이후 다른 도메인에서 비슷한 패턴이 나오면(예: 자원문서관리) 참고할 만한 선례로 남긴다.
- 옛 스펙 문서와의 겹침 판정은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md)에 기록했다(이 문서에서 실행하지 않음).

---

## 부록 A. 레퍼런스 스펙 (결정을 반영한 현재 상태)

> §1~12(분석·결정)의 **결과물**이다. 옛 [1-9_혼잡도관리.md](../1.spec/30_운영지원/1-9_혼잡도관리.md)에서 병합했다.

### A-1. 데이터 모델

**Facility**: `id, branchId(FK), name, type(GYM/POOL/GOLF/READING_ROOM/ETC), capacity, currentCount, level(1~5, computed), isActive(ADR-FAC-02, 미구현), lastUpdatedAt(ADR-FAC-01, 미구현)`

**CongestionSnapshot**(자동계산 이력, **미구현·Phase2**), **FacilityCheckIn**(QR 체크인, **미구현·Phase2**) — 1-9문서 원안에 있으나 Phase1 범위 밖.

### A-2. 화면 목록
| 화면 | 대상 | 상태 |
|---|---|---|
| 혼잡도 현황 | Member | admin-web에서 조회 가능, Member 앱은 Phase 3 미착수 |
| 시설 관리(마스터) | BRANCH_ADMIN | 구현됨 |
| 혼잡도 수동 보정 | BRANCH_ADMIN | 구현됨 |
| 시설 QR 체크인 스캐너 | Member(앱) | 미구현(Phase2) |
| 현재 이용자 목록 | BRANCH_ADMIN | 미구현(Phase2) |

### A-3. API 명세
| Method | Endpoint | 권한 | 구현 상태 |
|---|---|---|---|
| GET | /api/v1/facilities?branchId= | 전체 | 구현됨(currentCount/level 포함, 별도 congestion 엔드포인트로 안 나눔 — 단순화 결정) |
| POST/PATCH | /api/v1/facilities | BRANCH_ADMIN | 구현됨(DELETE는 ADR-FAC-02로 예정) |
| POST | /api/v1/facilities/:id/congestion/manual | BRANCH_ADMIN | 구현됨 |
| POST | /api/v1/facility-checkins | Member | 미구현(Phase2) |
| PATCH | /api/v1/facility-checkins/:id/checkout | Member/BRANCH_ADMIN | 미구현(Phase2) |
| GET | /api/v1/facilities/:id/current-users | BRANCH_ADMIN | 미구현(Phase2) |

### A-4. 권한 요약
- 회원/전체: 조회만
- BRANCH_ADMIN: 본인 지점 시설 관리 + 수동 보정

### A-5. 구현 우선순위
**Phase 1(완료)**: Facility CRUD, 수동 혼잡도 입력+조회, capacity 변경 시 즉시 재계산.
**Phase 2(미구현, 의도적 범위 제외)**: QR 체크인/아웃, 5분 주기 자동계산 스케줄러, 미체크아웃 자동 마감 배치, 현재 이용자 목록.
