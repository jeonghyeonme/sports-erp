# 도메인 사이클 — 예약및결제

> **상태**: 초안 (2026-09-22). [process/03_도메인_사이클_템플릿.md](../process/03_도메인_사이클_템플릿.md)의 11단계를 적용한 4호 사이클 — **상 우선순위 4개 도메인 중 마지막.**
> **대상 코드**: `apps/api/src/modules/reservations/`, `apps/api/src/modules/payments/`
> **이 문서가 예약및결제 도메인의 단일 문서다.** 옛 [1-7_예약및결제.md](../1.spec/20_이용자서비스/1-7_예약및결제.md)의 내용을 병합했다. 1-7문서는 **폐기 예정**(대체됨 배너 추가) — 6개 문서가 참조 중(그중 `4-3_3주차발표_슬라이드구성.md`는 발표자료 보호 영역)이라 링크 정리 후 삭제. 진행 상황은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md) 참고.
> **이 도메인이 원래 차별화 파일럿이었다**: 이 프로젝트에서 "엣지케이스를 얼마나 깊이 다뤘나"를 보여줄 대표 사례로 가장 먼저 꼽혔던 도메인이다(대화 초반 "팬텀 삽입" 논의 참고). 그런데 실제로 코드를 열어보니 **이 도메인 전체에 자동 테스트가 0건**이다(§11) — 가장 정교하게 설계된 로직이 가장 검증이 안 된 상태라는, 이 프로젝트 전체에서 가장 큰 문서-코드 괴리다.

## 0. 이 도메인 개요 (사용자 스토리)

- 회원으로서, 원하는 프로그램의 회차를 조회하고 예약하고 싶다.
- 회원으로서, 유료 회차는 예약 후 모의결제로 확정하고 싶다.
- 회원으로서, 사정이 생기면 예약을 취소하고(마감시간 전이면) 환불받고 싶다.
- 지점 관리자로서, 회원이 현장에 도착하면 체크인 처리하고 싶다.
- 지점 관리자로서, 우리 지점 예약·결제 현황을 조회하고 싶다(타 지점은 불가).

RFP는 "무/유료 프로그램 정보 게시 후 프로그램별 예약, 모바일 결제 기능"만 요구하고 "결제내역 처리방법은 추후 논의"로 남겼다(F06). 이 도메인은 그 공백을 **모의결제 + 상태 전이만 구현하되 외부 결제 연동 시 필요한 경계는 미리 준비**하는 방식으로 메운다(Q-05, §8 D3).

## 1. 요구사항 재분류

### 기능
| ID | 내용 |
|---|---|
| F06a | 무/유료 프로그램 정보 게시 후 프로그램별 예약 |
| F06b | 모바일 결제 기능 |
| F06c | 결제내역 처리방법(RFP 원문 "추후 논의") |

### 품질
| ID | 내용 |
|---|---|
| Q20 | 결제시스템은 금융·보안기관 시스템에 연계 가능 여부 확인 |

### 제약
- C01(과업 기간)·인프라 부재: 스케줄러가 없어 "5분 결제 미완료 자동취소", "노쇼 자동 처리"는 범위 제외(§8).

### 불변규칙 (협의 불가)
1. **동일 회차의 정원을 초과해 예약이 확정될 수 없다.** (영상의 "동일 좌석 중복판매 금지"와 완전히 같은 성격 — 몇 건이 초과되는 것도 협의 대상이 아니다)
2. **동일 회원이 같은 회차에 활성(`REQUESTED`/`CONFIRMED`) 예약을 중복으로 가질 수 없다.**
3. **위탁계약이 TERMINATED된 지점에는 신규 예약이 생성될 수 없다.** (CLAUDE.md 4대 불변식 — 계약 종료 지점 차단)
4. **결제 금액은 클라이언트가 아니라 서버가 Program 가격을 재조회해 결정한다.** (기본적인 결제 보안 원칙 — 협의 대상 아님)

## 2. 되묻기 확정

| ID | 질문 | 상태 | 확정 내용 |
|---|---|---|---|
| Q-05 | 결제 범위(F06b·c) — 실 PG 연동 vs 모의결제 | **이미 확정·구현됨** | 모의결제 + 상태 전이만. 외부 결제 장애 격리 설계는 유지해 실연동 전환 대비 — 이 사이클에서 D3으로 재확인 |

## 3. 수치화

| 항목 | 목표 | 근거 |
|---|---|---|
| 정원 초과 확정 예약 | 0건(신규 개설 회차에 정원만큼 동시 요청 시에도) | 불변규칙 1 |
| 중복 활성 예약 | 0건(취소 이력은 재예약 막지 않음) | 불변규칙 2 |
| 계약종료 지점 신규 예약 | 0건 통과 | 불변규칙 3 |
| 결제 금액 클라이언트 위조 | 0건 반영 | 불변규칙 4 |
| 취소 시 환불 여부 | `Branch.cancellationDeadlineHours`(기본 24시간) 기준 정확히 분기 | 1-7문서 §6 |
| VAT 분리 정확도 | `supplyAmount = round(amount/1.1)`, `vat = amount - supplyAmount`, 반올림 오차 없음 | 1-7문서 §6, 회계 기준 |

**[공용 산정 상수](../architecture/design-constants.md) 재사용**: 지점당 피크 동시 재실자 ≈33명(3년 후 전사 ≈4,300명). 이 값은 §6(Baseline)에서 "회차 오픈 순간 집중"과 "평균 재실"이 다른 시나리오임을 구분하는 근거로만 쓴다(새로 산정하지 않음) — 진짜 동시성 위험은 재실자 수가 아니라 "인기 회차 오픈 직후 수 초 내 몰리는 예약 요청 수"인데, 이 값은 아직 산정 근거가 없다(§12 이월).

## 4. Architecture Driver 식별 (3개로 제한)

| # | Driver | 왜 구조를 바꾸는가 |
|---|---|---|
| D1 | 정원 초과 방지 락 대상 | 락 대상을 잘못 고르면(카운트 대상) 팬텀 삽입으로 불변규칙 1이 뚫림 — 이 프로젝트의 "부분 unique/행 락" 패턴의 **원조** |
| D2 | 중복 예약 방지 강제 계층 | 애플리케이션 체크만으로는 경합 시 우회 가능 — DB 제약과의 이중화 여부 |
| D3 | 결제 모듈의 실연동 대비 경계 | 어댑터 유무가 나중에 실PG로 바꿀 때 예약 도메인 코드를 건드려야 하는지를 결정 |

**Driver로 승격하지 않은 것 (Tier2 — 규칙표로만 관리)**

| ID | 규칙 | 근거 | 처리 |
|---|---|---|---|
| RSV-T01 | 무료 회차(price=0)는 Payment 없이 즉시 CONFIRMED | 이미 구현됨, 구조 안 바뀜 | 구현 완료 |
| RSV-T02 | 취소 시 마감시간(기본 24h) 기준 환불 여부 분기 | 이미 구현됨 | 구현 완료 |
| RSV-T03 | VAT 분리(`round(amount/1.1)`) | 이미 구현됨, 계산 규칙 | 구현 완료 |
| RSV-T04 | 진행중(`RUNNING`) 아닌 프로그램은 예약 불가 | 이미 구현됨 | 구현 완료 |
| RSV-T05 | 5분 결제 미완료 자동취소 | 스케줄러 인프라 없음, 이미 범위 제외(1-7문서 §8) | **미구현, 검증 대상 아님** |
| RSV-T06 | 노쇼 자동 처리 | 동일 사유 | **미구현, 검증 대상 아님** |
| RSV-T07 | 강사 정산·원천징수 | 의도적 범위 제외(CLAUDE.md) | **범위 제외, 만들지 않음** |

## 5. Driver 우선순위

**불변규칙(§1) 항상 최우선** → D1(불변규칙 1 직접 구현, 이 도메인에서 가장 널리 알려진 리스크) → D2(불변규칙 2) → D3(아직 실연동이 없어 리스크는 낮지만 나중에 통째로 재작성하는 비용을 지금 막을 수 있음)

## 6. 현행 구조 진단 (Baseline)

**D1**: 1-7문서 §6이 이미 이 진단을 상세히 해 뒀다(2026-09-15 "락 대상 정정") — 그대로 가져온다. 기존 설계(`SELECT COUNT(*) FROM Reservation WHERE scheduleSlotId=? FOR UPDATE`)는 **이미 존재하는 행에만** 락을 건다. 정원이 비어 있는(기존 예약 0건) 회차에 두 트랜잭션이 동시에 이 쿼리를 실행하면 둘 다 잠글 대상이 없어 그대로 통과하고 둘 다 `INSERT`할 수 있다 — 팬텀 삽입. 이게 D1을 만든 원인이다.
현재 Mock 구현은 `MockDataService`가 단일 Node 프로세스에서 동기 실행되는 인메모리 배열이라는 점을 이용해, 별도 락 코드 없이도 팬텀 삽입이 원천적으로 안 일어난다(1-7문서 §8) — **실DB 전환 시 문서의 락 방식을 그대로 적용해야 한다**는 전제가 이미 문서화돼 있다.

**D2**: `createReservation`은 애플리케이션 레벨에서만 중복을 체크한다(`this.reservations.find(...)`). 실DB에서 이 체크와 `INSERT` 사이에 경합이 생기면(동시 요청 두 건이 모두 "중복 없음"을 확인한 뒤 둘 다 삽입) 뚫릴 수 있다.

**D3**: `mockPay`는 `MockDataService`의 평범한 동기 메서드일 뿐, 결제 게이트웨이를 추상화하는 인터페이스가 없다. 나중에 실제 PG를 붙이려면 이 메서드 내부를 통째로 바꿔야 하고, 타임아웃·재시도·서킷브레이커 개념이 들어갈 자리가 코드에 아예 없다.

## 7. 설계 질문 (Driver당 1개)

1. 정원이 비어 있는 회차에 동시 예약이 몰려도 초과 확정을 막으려면 무엇을 잠글까?
2. 중복 예약 방지를 애플리케이션 체크만으로 충분히 믿을 수 있을까?
3. 나중에 실제 결제 게이트웨이로 바꿀 때 예약 도메인 코드를 건드리지 않으려면 지금 무엇을 준비해 둬야 할까?

## 8. 대안 → 트레이드오프 → 결정

### 질문 1: 정원 초과 방지 락 대상

| 대안 | 장점 | 단점 |
|---|---|---|
| A. `SELECT COUNT(*) ... FOR UPDATE`(기존 설계, 카운트 대상 락) | 직관적 | **팬텀 삽입**(§6 baseline) — 신규 개설 회차에서만 뚫리는, 발견하기 어려운 결함 |
| **B. `SELECT * FROM ScheduleSlot WHERE id=? FOR UPDATE`로 회차 행을 먼저 잠근다 (채택)** | 회차 행은 예약이 0건이어도 항상 존재해 반드시 잠글 대상이 있음 — 두 번째 트랜잭션이 첫 번째 커밋까지 대기, 이후 `COUNT` 쿼리엔 `FOR UPDATE` 불필요(이미 직렬화됨) | 회차 단위로 락이 걸려 같은 회차의 다른 연산(예: 정원 수정)과 짧게 경합 가능 |
| C. 낙관적 락(버전 컬럼) | 경합 적을 때 처리량 높음 | 인기 회차는 경합이 집중돼 재시도 비용이 커짐 — 정원이 딱 채워지는 순간이 이 시스템의 정상 상태라 오히려 최악의 경우가 흔함 |

**결정**: B. 판단 기준: 불변규칙 1을 "항상" 만족시켜야 하는데 A는 신규 개설 회차라는 흔한 케이스에서 뚫린다. C는 이 도메인의 트래픽 패턴(인기 회차 집중)과 안 맞음.
**감수하는 것**: 지금(Mock)은 실측 불가능한 가설이다 — 실DB 전환 후 부하·동시성 테스트로 검증해야 한다(§11 "남은 것").

### 질문 2: 중복 예약 방지 강제 계층

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 애플리케이션 체크만(현행) | 구현됨, 단순 | 경합 시 우회 가능(§6 baseline) |
| **B. 애플리케이션 체크 + 부분 unique 인덱스(`Reservation(memberId, scheduleSlotId) WHERE status IN ('REQUESTED','CONFIRMED')`) 이중화 (채택)** | 이 프로젝트의 표준 패턴(회원관리 ADR-MEM-02, 인사정보관리 ADR-STF-03과 동일 원칙) — 이 도메인이 사실 그 패턴의 **원조**다. 취소 이력이 쌓여도 활성 상태만 걸리므로 재예약은 항상 허용 | MockDataService에는 부분 인덱스가 없어 지금은 A만으로 방어 |

**결정**: B. 판단 기준: 애플리케이션 체크 단독은 경합에 취약하다는 게 이미 1-7문서에 명시돼 있고, DB 레벨 이중화가 이 프로젝트의 검증된 표준.
**감수하는 것**: 지금은 가설이다 — 실DB 전환 시 적용.

### 질문 3: 결제 모듈의 실연동 대비 경계

| 대안 | 장점 | 단점 |
|---|---|---|
| A. 지금처럼 `mockPay`를 평범한 메서드로 유지, 실PG 전환 시 내부를 통째로 교체 | 지금 가장 적은 코드 | 나중에 타임아웃·재시도·서킷브레이커까지 한 번에 새로 설계해야 함 — 재작성 비용이 이연될 뿐 사라지지 않음 |
| **B. `PaymentGateway` 인터페이스를 지금 도입하고 `MockPaymentGateway` 구현체 하나만 꽂는다 (채택)** | 영상의 "결제 모듈을 어댑터로 분리" 결정과 동일 원리 — 실PG 추가 시 예약 도메인 코드는 안 건드림 | 지금 당장 쓰임새가 하나뿐인 추상화 계층(YAGNI 우려) |
| C. 요청은 동기/알림은 비동기로 분리(영상 원안) | 외부 장애 격리가 가장 강함 | 메시지 큐·이벤트 버스가 이 프로젝트에 없음 — 인프라 초과 |

**결정**: B. 판단 기준: 이 프로젝트는 "나중에 필요해지면 그때 확장"이 아니라 "지금 배관만 놓아두면 나중 비용이 적은 것"을 우선한 전례가 있다(1-7문서의 부가세 분리를 Phase 1로 앞당긴 것과 같은 태도). C는 인프라가 없어 배제.
**감수하는 것**: 지금은 구현체가 Mock 하나뿐이라 인터페이스의 가치가 눈에 보이지 않는다 — 실PG를 실제로 붙이기 전까지는 "왜 이렇게 했나"를 코드만 보고는 설득하기 어렵다(그래서 이 ADR이 필요하다).

## 9. 감수하는 것 (종합)

- D1·D2: 정확히 권한관리·인사정보관리 도메인과 같은 처지 — Mock 환경에서는 검증도 실효도 없는 가설이다.
- D3: 지금 당장 이득이 안 보이는 추상화를 미리 넣는 결정이라, 실PG 연동 전까지는 "과한 설계 아니냐"는 의문이 계속 남는다.
- **이 사이클 전체가 감수하는 것**: 가장 정교한 설계를 가진 도메인이 가장 테스트가 없다(§11) — Driver 결정의 품질과 검증 수준이 반비례하는 상태를 그대로 보고한다.

## 10. ADR

### ADR-RSV-01: 정원 초과 방지 — ScheduleSlot 행 락
- **컨텍스트**: 카운트 대상 락은 신규 개설 회차에서 팬텀 삽입으로 뚫림(§6 baseline)
- **검토한 대안**: A 카운트 FOR UPDATE / B 회차 행 FOR UPDATE / C 낙관적 락
- **결정**: B. 항상 잠글 대상이 있음 + 인기 회차 트래픽 패턴과 부합
- **결과**: 좋아진 것 — 신규 개설 회차 포함 모든 경우에 정원 보장 / 감수하는 것 — 실측 전 가설 / 숙제 — 실DB 전환 후 부하·동시성 테스트

### ADR-RSV-02: 중복 예약 방지 — 애플리케이션 체크 + 부분 unique 인덱스 이중화
- **컨텍스트**: 애플리케이션 체크 단독은 경합 시 우회 가능(§6 baseline)
- **검토한 대안**: A 애플리케이션만 / B 이중화
- **결정**: B. 이 프로젝트의 표준 패턴(회원관리·인사정보관리와 동일 원칙) — 이 도메인이 원조
- **결과**: 좋아진 것 — DB가 최종 방어선 / 감수하는 것 — 지금은 A만 실효 / 숙제 — 실DB 전환 시 적용

### ADR-RSV-03: 결제 모듈 — PaymentGateway 어댑터 인터페이스 도입
- **컨텍스트**: 실PG 전환 시 예약 도메인까지 재작성해야 하는 구조(§6 baseline)
- **검토한 대안**: A 현행 유지 / B 어댑터 인터페이스 / C 동기/비동기 분리
- **결정**: B. "나중 비용을 줄이는 배관을 지금 놓는다"는 이 프로젝트의 기존 태도와 일치, C는 인프라 초과
- **결과**: 좋아진 것 — 실PG 추가 시 예약 도메인 무변경 / 감수하는 것 — 지금은 가치가 안 보이는 추상화 / 숙제 — 없음(구현만 남음)

## 11. 검증 현황 (정직하게 보고)

| 항목 | 상태 |
|---|---|
| 계약종료 지점 신규 예약 차단(불변규칙 3) | **검증됨** — `contract-termination.spec.ts` |
| BRANCH_ADMIN 타 지점 예약 취소/체크인 시 403 | **검증됨** — `branch-isolation.spec.ts` |
| **정원 초과 방지(불변규칙 1, D1)** | **검증됨(2026-09-22)** — `apps/api/test/reservation-capacity.spec.ts`. 신규 개설(예약 0건) capacity=2 회차에 3명 동시 요청 → 정확히 2건 성공·1건 409(SLOT_FULL) — **바로 이 팬텀 삽입 케이스를 위해 시드돼 있던 `slot-seocho-yoga-2`를 처음으로 사용** |
| **중복 예약 방지(불변규칙 2, D2)** | **검증됨(2026-09-22)** — 동일 파일(중복 시도 409 ALREADY_RESERVED + 취소 후 재예약 성공) |
| **모의결제 승인·환불 흐름** | **검증됨(2026-09-22)** — 동일 파일(승인→APPROVED/CONFIRMED, PENDING 취소→FAILED/CANCELLED) |
| **취소 정책(24시간 기준 환불 분기)** | **검증됨(2026-09-22)** — 동일 파일(마감 이내 취소=환불 없음, 마감 이전 취소=REFUNDED 양쪽 분기) |
| VAT 분리 계산 정확도 | **검증됨(2026-09-22)** — 동일 파일(price 30000 → supplyAmount 27273, vat 2727) |
| D3(PaymentGateway 어댑터) | **구현 안 됨 — 검증 대상 아님**(ADR만 존재) |

**다음 행동 제안 — 1~3 완료(2026-09-22)**: 이 도메인은 다른 세 "상" 도메인과 달리 결정과 구현은 이미 있고 테스트만 전무했다는 점이 달랐다.
1. ~~정원 초과 방지 테스트(일반 + 신규 개설 회차 팬텀 삽입 케이스)~~ → `reservation-capacity.spec.ts`로 완료 — 이 도메인의 존재 이유를 처음으로 증명했다.
2. ~~중복 예약 방지 + 취소 후 재예약 허용~~ → 동일 파일로 완료.
3. ~~모의결제 승인→환불 흐름 + 취소 정책 24시간 분기~~ → 동일 파일로 완료.
4. D3(PaymentGateway 인터페이스 추출)은 구현 자체가 필요한 리팩터링이라 별도 작업 — **미착수**.

## 12. 이월 사항 + 문서 마이그레이션 메모

- **"회차 오픈 순간 집중" 부하 산정 공백**: §3에서 언급한 대로, 공용 산정 상수의 "평균 동시 재실자"는 이 도메인의 진짜 위험(인기 회차 오픈 직후 수 초 집중)과 다른 지표다. 별도 산정이 필요하면 [architecture/design-constants.md](../architecture/design-constants.md)에 이어서 다루되, 지금은 실측 근거가 없어 보류한다.
- 옛 스펙 문서와의 겹침 판정은 [process/05_문서_마이그레이션_추적.md](../process/05_문서_마이그레이션_추적.md)에 기록했다(이 문서에서 실행하지 않음).

---

## 부록 A. 레퍼런스 스펙 (결정을 반영한 현재 상태)

> §1~12(분석·결정)의 **결과물**이다. 옛 [1-7_예약및결제.md](../1.spec/20_이용자서비스/1-7_예약및결제.md)에서 병합했다.

### A-1. 데이터 모델

**ScheduleSlot**(프로그램 회차): `id, programId(FK), date, startTime, capacity`
**Reservation**: `id, memberId(FK), scheduleSlotId(FK), status(REQUESTED/CONFIRMED/CANCELLED/COMPLETED/NO_SHOW), createdAt, cancelledAt, cancelReason`
**Payment**(매출 인식의 원천 — 예약 결제 + PT 패키지 구매 결제 통합): `id, reservationId(FK, nullable — PT 패키지는 ptSessionId), memberId, amount, supplyAmount, vat, method, status(PENDING/APPROVED/FAILED/REFUNDED), mockApprovalNo, approvedAt, refundedAt`

### A-2. API 명세
| Method | Endpoint | 권한 | 설명 | 구현 상태 |
|---|---|---|---|---|
| POST | /api/v1/reservations | MEMBER | 예약 생성 | 구현됨 |
| GET | /api/v1/reservations | MEMBER/BRANCH_ADMIN/SUPER_ADMIN | 목록(스코프별) | 구현됨 |
| PATCH | /api/v1/reservations/:id/cancel | MEMBER/BRANCH_ADMIN | 취소 | 구현됨 |
| PATCH | /api/v1/reservations/:id/check-in | BRANCH_ADMIN | 체크인 | 구현됨 |
| POST | /api/v1/payments/:reservationId/mock-pay | MEMBER | 모의결제 승인 | 구현됨 |
| GET | /api/v1/payments | BRANCH_ADMIN/SUPER_ADMIN | 결제 목록(매출) | 구현됨 |

### A-3. 권한 요약
- 회원은 본인 예약만 생성/취소/조회
- BRANCH_ADMIN은 본인 지점 프로그램에 연결된 예약만 조회/체크인/취소
- 결제 금액은 서버 재조회(불변규칙 4)
- 강사 정산 생성·확정·지급은 SUPER_ADMIN 전용이지만 **범위 제외**(RSV-T07) — 화면 자체가 없음

### A-4. 구현 우선순위
**Phase 1(완료)**: 회차 조회, 예약 생성/취소, 무료 회차 흐름, 정원 초과 방지(동시성 처리) — 실DB 전환 시 ADR-RSV-01 락 방식 적용 필요.
**Phase 2(완료)**: 모의결제 승인/환불, 취소 정책(24시간).
**Phase 6(완료)**: VAT 분리.
**범위 제외**: 5분 결제 미완료 자동취소, 노쇼 자동 처리(스케줄러 없음), PT 패키지 Payment 생성(PTSession 엔티티 없음), 매출 집계 API, 강사 정산·원천징수.
