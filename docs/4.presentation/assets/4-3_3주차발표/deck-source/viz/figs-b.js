// 3부 도식(앞, 간추림): 슬라이드 9, 10, 11, 12
const { txt, box, arrow, line, rect, svg, col } = require('./lib');

// ── 슬라이드 9: 지점 격리 — 요청이 통과하는 세 관문 ──────────────────────────
function f09() {
  return svg('f09', 1200, 320, '요청은 로그인, 역할, 지점의 세 관문을 차례로 통과해야 하고, 다른 지점의 데이터는 마지막 관문에서 막힌다', () => {
    let s = '';
    s += txt(20, 34, '요청이 데이터에 닿기까지', { size: 20, w: 700 });
    s += box(20, 96, 130, 96, [{ t: 'API 요청', s: 20, w: 700 }], { kind: 'plain' });
    const gates = [
      ['로그인 확인', '토큰(JWT) 검증', '401 차단', 'gray'],
      ['역할 확인', '이 API를 쓸 수 있는\n역할인가', '403 차단', 'gray'],
      ['지점 확인', '요청한 데이터가\n내 지점의 것인가', '403 다른 지점 차단', 'blue'],
    ];
    gates.forEach(([name, desc, err, kind], i) => {
      const x = 200 + i * 300;
      s += box(x, 84, 250, 120, [{ t: name, s: 22, w: 700 }, { t: desc, s: 16, w: 400, c: 'ink' }], { kind, sw: 2 });
      s += arrow([[x - 50, 144], [x, 144]], { color: 'ink', sw: 2.5 });
      s += arrow([[x + 125, 204], [x + 125, 246]], { color: 'red', dash: true });
      s += box(x + 20, 246, 210, 44, [{ t: err, s: 15, w: 700 }], { kind: 'red', r: 8, sw: 1.4 });
    });
    s += arrow([[1050, 144], [1090, 144]], { color: 'ink', sw: 2.5 });
    s += box(1090, 104, 90, 80, [{ t: '데이터', s: 19, w: 700 }], { kind: 'green' });
    s += `<path d="M800 68 L800 60 L1050 60 L1050 68" fill="none" stroke="${col('blue')}" stroke-width="2"/>`;
    s += txt(925, 50, '지점 격리 — 가드와 컨트롤러가 이중으로 검사', { size: 16, w: 700, fill: 'blue', anchor: 'middle' });
    return s;
  });
}

// ── 슬라이드 10: 인사·근태 ─────────────────────────────────────────────────
function f10() {
  return svg('f10', 1200, 560, '직원은 본사가 채용해 지점에 파견하고, 근태는 출근 시각으로 정상·지각을 가리며, 연차만 승인 시 잔여일수에서 차감된다', () => {
    let s = '';
    // 밴드 A: 채용·파견
    s += txt(20, 62, '채용 · 파견', { size: 22, w: 700 });
    s += txt(20, 88, '직원은 본사 소속', { size: 15, fill: 'muted' });
    s += box(300, 30, 200, 66, [{ t: '본사', s: 22, w: 700 }], { kind: 'blue' });
    s += box(600, 30, 200, 66, [{ t: '직원', s: 22, w: 700 }], { kind: 'green' });
    s += box(900, 30, 200, 66, [{ t: '지점', s: 22, w: 700 }], { kind: 'blue' });
    s += arrow([[500, 63], [600, 63]], { color: 'blue', sw: 2.5, label: { x: 550, y: 52, t: '채용', s: 16 } });
    s += arrow([[800, 63], [900, 63]], { color: 'green', sw: 2.5, label: { x: 850, y: 52, t: '파견', s: 16 } });
    s += txt(300, 128, '재배치도 본사만 할 수 있고, 이전 파견은 기록으로 남는다', { size: 17, fill: 'ink', w: 500 });
    s += line(20, 156, 1180, 156, { c: 'line', sw: 1.5 });

    // 밴드 B: 출근 판정
    s += txt(20, 232, '출근 체크인', { size: 22, w: 700 });
    s += txt(20, 258, '지점 기준시각 대비 자동 판정', { size: 15, fill: 'muted' });
    s += box(300, 190, 200, 66, [{ t: '출근 체크인', s: 21, w: 700 }], { kind: 'plain' });
    s += box(660, 174, 240, 56, [{ t: '정상', s: 21, w: 700 }, { t: '기준시각 + 10분 이내', s: 14, w: 400, c: 'ink' }], { kind: 'green' });
    s += box(660, 246, 240, 56, [{ t: '지각', s: 21, w: 700 }, { t: '기준시각 + 10분 초과', s: 14, w: 400, c: 'ink' }], { kind: 'amber' });
    s += arrow([[500, 214], [660, 202]], { color: 'green', sw: 2.5 });
    s += arrow([[500, 232], [660, 274]], { color: 'amber', sw: 2.5 });
    s += line(20, 328, 1180, 328, { c: 'line', sw: 1.5 });

    // 밴드 C: 휴가
    s += txt(20, 430, '휴가 · 연차', { size: 22, w: 700 });
    s += txt(20, 456, '연차만 잔여일수를 차감', { size: 15, fill: 'muted' });
    s += box(300, 392, 200, 66, [{ t: '휴가 신청', s: 21, w: 700 }], { kind: 'amber' });
    s += box(600, 392, 220, 66, [{ t: '지점 관리자 승인', s: 20, w: 700 }], { kind: 'green' });
    s += box(920, 380, 260, 90, [{ t: '연차이면\n잔여일수에서 차감', s: 19, w: 700 }, { t: '병가·경조는 차감 없음', s: 14, w: 400, c: 'ink' }], { kind: 'blue' });
    s += arrow([[500, 425], [600, 425]], { color: 'green', sw: 2.5 });
    s += arrow([[820, 425], [920, 425]], { color: 'blue', sw: 2.5 });
    return s;
  });
}

// ── 슬라이드 11: 회원·강사·프로그램 ─────────────────────────────────────────
function f11() {
  return svg('f11', 1200, 560, '지점 아래에 회원·강사·프로그램이 있고, 프로그램은 정해진 순서로만 상태가 바뀌며 진행중일 때만 예약할 수 있다', () => {
    let s = '';
    s += txt(20, 40, '지점 단위로 묶이는 데이터', { size: 20, w: 700 });
    s += box(20, 64, 150, 60, [{ t: '지점', s: 22, w: 700 }], { kind: 'blue' });
    s += arrow([[170, 94], [230, 94]], { color: 'blue', sw: 2.5 });
    ['회원', '강사', '프로그램'].forEach((t, i) => {
      s += box(230 + i * 190, 64, 160, 60, [{ t, s: 21, w: 700 }], { kind: 'green' });
    });
    s += txt(820, 100, '프로그램은 회차로 나뉘어 예약된다', { size: 16, fill: 'muted' });

    s += txt(20, 200, '프로그램 상태 — 허용된 전이만 통과', { size: 20, w: 700 });
    s += box(60, 290, 200, 84, [{ t: '준비중', s: 24, w: 700 }], { kind: 'gray' });
    s += box(400, 290, 200, 84, [{ t: '진행중', s: 24, w: 700 }], { kind: 'green' });
    s += box(760, 290, 200, 84, [{ t: '종료', s: 24, w: 700 }], { kind: 'red', term: true });
    s += box(400, 450, 200, 84, [{ t: '휴강', s: 24, w: 700 }], { kind: 'amber' });
    s += arrow([[260, 332], [400, 332]], { color: 'green', sw: 3, label: { x: 330, y: 318, t: '개시', s: 16 } });
    s += arrow([[600, 332], [760, 332]], { color: 'gray', sw: 3, label: { x: 680, y: 318, t: '종료', s: 16 } });
    s += arrow([[160, 290], [160, 236], [860, 236], [860, 290]], { color: 'gray', sw: 2, label: { x: 510, y: 228, t: '바로 종료도 가능', s: 15 } });
    s += arrow([[470, 374], [470, 450]], { color: 'amber', sw: 2.5, label: { x: 458, y: 418, t: '중지', a: 'end', s: 16 } });
    s += arrow([[530, 450], [530, 374]], { color: 'green', sw: 2.5, label: { x: 542, y: 418, t: '재개', a: 'start', s: 16 } });
    s += arrow([[600, 492], [860, 492], [860, 374]], { color: 'gray', sw: 2, label: { x: 730, y: 482, t: '종료', s: 15 } });
    s += box(400, 246, 200, 34, [{ t: '예약은 이 상태에서만', s: 15, w: 700 }], { kind: 'green', r: 17, sw: 1.4 });
    s += txt(1060, 336, '종결\n되돌릴 수 없음', { size: 16, fill: 'muted', anchor: 'middle' });
    return s;
  });
}

// ── 슬라이드 12: 예약·결제 (한 장) ──────────────────────────────────────────
function f12() {
  return svg('f12', 1200, 560, '예약은 결제를 거쳐 확정되고, 취소하면 회차 시작 24시간 전을 기준으로 환불 여부가 갈린다', () => {
    let s = '';
    s += txt(20, 34, '예약 흐름', { size: 20, w: 700 });
    const steps = [['회차 선택', 'plain', ''], ['예약', 'plain', '진행중 · 정원 · 중복 확인'], ['결제', 'green', '모의 결제'], ['예약 확정', 'green', ''], ['이용 체크인', 'blue', '지점 관리자 확인']];
    steps.forEach(([t, kind, sub], i) => {
      const x = 20 + i * 234;
      s += box(x, 92, 190, 84, [{ t, s: 22, w: 700 }], { kind, sw: 2 });
      if (sub) s += txt(x + 95, 202, sub, { size: 14, anchor: 'middle', fill: 'muted', w: 500 });
      if (i < 4) s += arrow([[x + 190, 134], [x + 234, 134]], { color: 'green', sw: 2.5 });
    });
    s += arrow([[275, 92], [275, 62], [794, 62], [794, 92]], { color: 'gray', dash: true, label: { x: 534, y: 54, t: '무료 프로그램은 결제 없이 바로 확정', s: 15, c: 'muted' } });

    s += txt(20, 290, '취소하면 — 회차 시작 24시간 전이 기준', { size: 20, w: 700 });
    s += rect(60, 340, 700, 64, { fill: 'greenBg', stroke: 'green', r: 8, sw: 1.8 });
    s += txt(410, 380, '전액 환불', { size: 22, w: 700, fill: 'green', anchor: 'middle' });
    s += rect(760, 340, 380, 64, { fill: 'amberBg', stroke: 'amber', r: 8, sw: 1.8 });
    s += txt(950, 380, '환불 없음', { size: 22, w: 700, fill: 'amber', anchor: 'middle' });
    s += line(60, 404, 60, 432, { c: 'ink', sw: 2 }) + txt(60, 458, '예약', { size: 16, anchor: 'middle', fill: 'muted' });
    s += line(760, 322, 760, 432, { c: 'ink', sw: 2.5 }) + txt(760, 458, '회차 시작 24시간 전', { size: 18, anchor: 'middle', w: 700 });
    s += line(1140, 404, 1140, 432, { c: 'ink', sw: 2 }) + txt(1140, 458, '회차 시작', { size: 16, anchor: 'middle', fill: 'muted' });
    s += txt(60, 512, '기준 시간은 지점마다 설정할 수 있다 (기본 24시간)', { size: 16, fill: 'muted' });
    return s;
  });
}

module.exports = { f09, f10, f11, f12 };
