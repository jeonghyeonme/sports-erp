# 4-3. 3주차 진행상황 발표 — 슬라이드 구성안 (확정)

> 2주차 발표([4-2](4-2_2주차발표_슬라이드구성.md), 2026-09-14) 이후 커밋을 분석해 정리한 3주차 발표용 슬라이드 구성안입니다. 2026-09-20 세션의 초안을 2026-09-21 세션에서 **15분 발표 기준 17장으로 압축·확정**했습니다. 슬라이드 비주얼 제작 전 구성(내용) 확정까지가 이 문서의 범위입니다.
>
> 다루는 커밋 범위: `4f0fe5f`(2026-09-17)부터 `abf8caf`(2026-09-20)까지 15개 커밋. 9/14의 `ccd2310`·`ee06568`은 2주차 발표 준비 커밋이라 제외합니다. **`abf8caf` 이후 커밋(가드레일·lint 복구·CI·통합 테스트·격리 누수 수정 등)은 발표 범위에서 제외하기로 했습니다(2026-09-21 결정)** — 슬라이드에는 넣지 않고 §5의 Q&A 대비로만 둡니다.

## 0. 발표 조건 (2주차와 동일 가정 — 변경 시 갱신할 것)

- 형식: PPT/스크린샷 위주, 라이브 데모 없음
- **분량: 15분 내외** → 17장(표지·마무리 제외 실질 15장, 슬라이드당 평균 약 50초). 시간 배분은 §2 하단 표 참고
- 청중은 이전 발표 내용을 상세히 기억하지 못한다는 전제 → 3번 슬라이드에서 짧게 리마인드
- **청중: 개발 지식이 있는 분들** (2026-09-21 확정). 지점 격리·상태 전이·24시간 환불 기준 같은 핵심 개념은 남기되, 클래스명·오류 코드·영문 상태값·내부 필드는 슬라이드에 쓰지 않는다
- **도식 원칙**: 도식 하나에 메시지 하나, 요소 6개 안팎, 한글 업무 용어. **동작하는 것만 그린다.** 미구현·의도적 제외는 도식에 넣지 않고 슬라이드 8의 표와 §5의 Q&A로만 다룬다
- 회원 앱(React Native)이 미착수라, 회원 시연 화면은 **회원 계정으로 로그인한 admin-web 화면**입니다 — 슬라이드에 "회원 앱 화면 대용"이라고 명시할 것

## 1. 발표 축 (네 가지)

1. **실무 분석** — 회사의 실무가 어떻게 굴러가는지 직접 조사·분석해 정리했다 ([5-1 기업 분석 자료](../5.deliverables/5-1_기업분석자료.html))
2. **도메인 확장과 확장의 판단** — 그 분석에서 무엇을 넣고 무엇을 뺐는가, 그 기준은 무엇인가
3. **도메인별 구현 시연** — 실제 화면에서 동작하는 모습
4. **도메인별 구조의 시각화** — 각 도메인이 어떤 엔티티·상태·권한으로 짜여 있는가

전체 서사: **실무를 이해했다 → 그 이해가 설계의 근거가 됐다 → 구현할 범위를 판단했다 → 이렇게 동작한다**

## 2. 슬라이드 구성 (총 17장 / 도입 + 3부 + 마무리)

### 도입 (1~3장)

**1. 표지** — 프로젝트명, 발표자, 날짜, "3주차 진행상황 발표"

**2. 오늘 발표 한 줄 요약**
지난 발표 이후 네 갈래: ① 회사 실무를 분석했고 ② 그 분석으로 도메인을 넓혔고 ③ 넓힘과 자름의 기준을 세웠고 ④ 모든 도메인을 화면에서 동작하게 만들었다.

**3. 지난 발표 리마인드 + 이번 주 변화량**
- 사업 구조 다이어그램(본사↔위탁센터 계약 / 위탁센터↔직원↔이용자 운영) 한 장 — [1-1문서](../1.spec/00_공통/1-1_공통설계서.md) §2-1
- 2주차 종료(9/10, `5fc04bc`) 대비: API 엔드포인트 **23 → 68**, admin-web 페이지 **9 → 16**, mock 엔티티 **8 → 19** (2026-09-21 코드에서 재실측해 일치 확인)
- 2주차 때 "근태·예약결제는 설계만"이었던 것이 이번에 구현됨

### 1부. 실무 분석 — 회사가 실제로 어떻게 굴러가는가 (4~6장)

**4. 왜, 무엇을 분석했나 (5-1 기업 분석 자료 소개)**
- RFP 과업1이 시스템 설계에 앞서 "경영시스템·관리시스템 분석"을 선행 과업으로 요구 → 산출물2 "기업 분석 자료"
- 분석서 구성 캡처: **6개 도메인 × (실무 상세 → 분석 결과 → 결론)** 3단 구조 — 도메인 하나만 읽어도 완결되도록 설계. 도식의 예시는 **위탁계약**(계약이 끝나면 현장에서 무슨 일이 생기나 → 종료된 지점에 신규 활동이 들어오면 안 된다 → 지점에 계약 상태를 두고 종료 시 신규 활동을 막는다)
- 6개 도메인: 사업 구조 / 위탁계약 라이프사이클 / 인력 운용(파견·근태·연차) / 예약·결제·정산 / 물적자원관리 / 문서관리
- 5-1 HTML 화면 캡처 + [보기 좋은 버전](https://claude.ai/code/artifact/460e474f-2ded-4031-ae5b-07cf1efa3731) 링크(QR 후보)

**5. 실무 지도 (시각화)**
- 왼쪽: 사업 구조 흐름(본사 → 위탁센터 → 파견 직원 → 이용자, 계약 관계 vs 운영 관계 구분)
- 오른쪽: 위탁계약 상태(정상 → 갱신임박 → 만료/종료) + **"계약이 종료된 지점에서는 신규 회원 등록 · 신규 예약 · 게시글 신규 작성이 막힌다"** (과거 데이터 조회는 유지). 동작하는 3가지만 보여준다.
- 미구현 2곳(지점 소속 계정 처리 — 정책 미정, 파견 직원 종료·재배치)은 도식에 넣지 않고 **슬라이드 8의 "남긴 것"**에서 다룬다. "계약이 끝나면 막아야 하는 곳 5개"라는 표현은 슬라이드에서 쓰지 않는다(§5-4).
- 핵심 메시지: "우리 사업을 정확히 이해하고 있다" — 계약 종료가 현장 동작에 미치는 영향을 분석했고, 그중 신규 회원·예약·게시글 차단은 실제로 동작한다.

**6. 실무 분석 → 설계 → 구현 추적 (이 발표의 핵심 슬라이드)**
분석에서 발견한 공백이 실제 설계와 코드로 이어졌음을 표로 보여줌. 발표에서는 아래 후보 중 **동작하는 4~5행**만 고를 것(도식은 계약 현장 · 본사 채용·파견 · 물적자원 공백 · 동시 예약 4행). 부가세 행은 2026-09-21에 발표 슬라이드에서 빼고 백업으로 둔다(§5-6). 🔲·⬜ 행(마지막 3행)은 슬라이드에 넣지 않는다.

| 실무에서 발견한 것 | 설계 결정 | 구현 상태 |
|---|---|---|
| 지점은 매장이 아니라 **계약 현장**(초기 설계엔 계약 정보가 없었음) | 계약상대방·기간·상태 4단계 엔티티로 재정의 | ✅ 계약 정보 화면, 종료 지점 신규 등록·예약·게시 차단 |
| 직원은 지점 채용이 아니라 **본사 채용·현장 파견** | `StaffAssignment`, 채용·재배치는 본사 전용 | ✅ 파견 이력 포함 |
| 휴가는 종류마다 차감 방식이 다름(연차만 차감) | 유형별 처리 | ✅ 연차만 잔여일수 차감 |
| 총액에서 부가세를 역산하는 것이 표준 | 공급가액 = 결제금액÷1.1 | ✅ 27,273 + 2,727 = 30,000 — **백업**(발표 슬라이드에서는 제외, §5-6) |
| 동시 예약에서 **팬텀 삽입**(빈 회차엔 잠글 대상이 없음) | 예약 행이 아니라 회차 행을 먼저 잠금 | ✅ 동작(동기 mock이라 락 없이 안전, DB 전환 시 락 복원) |
| 물적자원관리가 **설계 전체의 공백**이었음 | 자원문서관리 도메인 신설, 100만원 기준 자동 분류 | ✅ Phase 1 (재물조사는 보류) |
| 첨부파일과 법적 증빙 문서는 성격이 다름 | 별도 `Document` 모델 + 보존기한 자동계산 | ✅ 인사서류 +3년, 계약서 수동 |
| 정기휴무를 반영하지 않으면 결근 **오탐**이 매주 발생 | 결근 판정에 정기휴무·고용형태 조건 | 🔲 분석·설계 완료, 자동 배치는 **의도적 제외**(스케줄러 없음) |
| 프리랜서 강사 정산은 3.3% 원천징수 후 지급 | 월정산·원천징수 모델 | 🔲 분석 유지, 구현은 **의도적 제외**(RFP 요구 아님) |
| PT 패키지는 구매 시점에 결제(매출)가 잡혀야 함 | 구매 시점 Payment 생성 | ⬜ PT세션 자체가 미착수 |

- 핵심 메시지: **분석이 설계를 이끌었고, 구현 범위는 그 위에서 판단했다** — 슬라이드에 넣지 않는 마지막 3행(제외·미구현)은 다음 2부의 "뺀 것·남긴 것"으로 이어진다

### 2부. 도메인 확장과 확장의 판단 (7~8장)

**7. 확장 지도 + 판단 프레임 (시각화)**
왼쪽 — 도메인 허브 다이어그램: RFP 원본 8개 기능 도메인을 한 색으로, 이번에 확장한 것을 다른 색으로 표시.
- 신설 도메인: **자원문서관리**(자산·비품 + 문서함) — 실무 분석(슬라이드 6)에서 드러난 공백 ([1-10문서](../1.spec/40_자원문서관리/1-10_기업구조및자원관리분석.md))
- 도메인 내 확장: 강사(`Instructor`) 엔티티, 프로그램 회차(`ScheduleSlot`), 위탁계약 라이프사이클(2주차부터) — 도식에서는 "자원문서관리 신설"만 강조하고 이 세 가지는 "그 밖에 강사·회차·계약 관리를 도메인 안에서 확장" 한 줄로 처리
- 근거 자료: [5-2 개발작업계획서](../5.deliverables/5-2_개발작업계획서.html)의 도메인 허브 다이어그램 재사용 가능

오른쪽 — 넣을지 뺄지를 가르는 네 가지 질문 흐름도:
1. RFP가 요구했는가? → 예: 무조건 대상
2. 없는 인프라(스케줄러·알림·파일 스토리지)가 필요한가? → 예: 보류/제외
3. 화면에서 눈에 보이는가? → 아니오: 제외 후보
4. 다른 기능과 중복되는가? → 예: 제외
- 근거 자료: [2-3 추적표 §2-4](../2.decisions/60_분석및제안/2-3_요구사항추적표.md)
- 포인트: **실무 분석서는 "회사가 실제로 하는 일 전체"를 그린 지도이고, 이 프레임은 그중 우리가 만들 범위를 고르는 기준**이다

**8. 판단 사례 — 넣은 것 / 뺀 것 / 남긴 것 (3열 한 장)**

| 넣은 것 — 왜 | 뺀 것 — 왜 | 남긴 것 — 정직한 갭 |
|---|---|---|
| 자원문서관리: RFP가 "분석"을 요구한 영역인데 물적자원이 통째로 비어 있었음 → Phase 1만 | 강사 정산·원천징수 — RFP 요구 아님, 코드에서도 제거 | 회원 PT 잔여세션·수강내역 |
| 회차(`ScheduleSlot`): 예약이 성립하려면 필수 선행 엔티티 | 혼잡도 QR 체크인·5분 자동계산 — 수동 보정으로 요건 충족, 앱·스케줄러 없음 | 인사 변경 신청 승인 |
| | 노쇼 자동처리·결제 TTL·자동 결근 — 스케줄러 없음 | AuditLog, 응답 마스킹 |
| | 감가상각 배치 — 회계 모듈이 없어 받는 곳이 없음 | 게시판 교육자료 첨부 |
| | | 계약 종료 시 파견 직원·소속 계정 정리 — 설계엔 있으나 구현 전(슬라이드 5에서 옮겨 옴) |

- 핵심 메시지: **"분석은 끝까지 했고, 구현은 판단해서 잘랐다. 안 만든 것도 문서에 근거와 확장 방향을 남겼다. RFP 요구인데 아직 없는 것은 숨기지 않는다."**
- 이 장에서 §5-1번의 문장("분석서에는 실무 전체를 명시하되, 구현을 진행하면서 불필요하다고 판단한 것은 제외하는 방향으로 진행했다")을 그대로 쓴다. 지역관리자(SV) 계층은 분석에서 발견했지만 RFP에 없어 일부러 설계에 넣지 않은 사례로 "뺀 것" 열에 한 줄 언급 가능

### 3부. 도메인 구현 시연 + 구조 시각화 (9~15장)

> 도메인 슬라이드는 공통 템플릿: **왼쪽 = 구조 시각화, 오른쪽 = 시연 스크린샷 2~3장(순서 화살표) + 핵심 한 줄 + "실무 분석서의 어느 부분에서 왔는가" 태그**

**9. 공통 뼈대 — 권한과 지점 격리 (시각화 중심)**
- 시각화 ①: 4개 역할 × 접근 범위 표(회원 정보 · 직원·인사 · 예약·결제 · 게시판 · 자산·문서 5개 영역)
- 시각화 ②: 요청이 통과하는 **3관문(로그인 → 역할 → 지점)**. 다른 지점의 데이터는 마지막 관문에서 막히고, 지점 격리는 가드와 컨트롤러가 이중으로 검사한다(클래스명·오류 코드는 슬라이드에 쓰지 않음)
- 이어지는 모든 도메인이 이 뼈대 위에 있다는 점을 먼저 보여줌. 사이드바가 역할별로 다르게 구성된 화면 대비 캡처 포함.

**10. 인사·근태** (분석서 도메인 02)
- 시각화(3구간): **채용 · 파견**(본사 → 직원 → 지점, 재배치도 본사만, 이전 파견은 기록으로 남음) / **출근 체크인**(지점 기준시각 +10분 이내면 정상, 초과하면 지각) / **휴가 · 연차**(신청 → 지점 관리자 승인 → 연차만 잔여일수 차감, 병가·경조는 차감 없음)
- 조퇴·결근·휴가 상태값은 정의만 있고 자동 판정 코드가 없어 도식에 넣지 않는다("근태 상태 5종"이라고 말하지 말 것)
- 시연: 체크인(지각 자동판정) → 휴가 신청 → 지점 관리자 승인 → 잔여연차 감소

**11. 회원 · 강사 · 프로그램**
- 시각화: 지점 아래에 회원 · 강사 · 프로그램이 묶인다는 한 줄 구조 + **프로그램 상태 전이도**(준비중 → 진행중 ⇄ 휴강 → 종료, 준비중·휴강에서 바로 종료 가능, 종료는 되돌릴 수 없음). 예약은 진행중일 때만 가능
- 시연: 회원 등록(미성년 생년월일 입력 시 보호자 동의 요구) → 프로그램 등록(자유이용 선택 시 가격·정원 비활성) → 상태 전이 드롭다운

**12. 예약·결제 ① 구조 (시각화 중심)** (분석서 도메인 03)
- 예약 흐름(도식 1장): 회차 선택 → 예약(진행중·정원·중복 확인) → 결제(모의) → 예약 확정 → 이용 체크인. 무료 프로그램은 결제를 건너뛰고 바로 확정
- 취소 정책 타임라인: 회차 시작 24시간 전 기준 전액 환불 / 환불 없음 (기준 시간은 지점별 설정, 기본 24시간)
- 상태 전이도 두 개(예약·결제)와 엔티티 흐름은 도식에서 뺐다(내부 상태값이 많아 청중에게 불필요)
- 동시성 판단 한 줄(구두): 문서는 DB 행 락, 현재는 동기 실행 mock이라 별도 락 없이 안전, DB 전환 시 락 복원

**13. 예약·결제 ② 시연**
회원 예약 → 결제 대기 → 모의결제 → 확정 → 취소(자동 환불) → 재예약 / 지점 관리자 체크인·결제내역

**14. 게시판 · 혼잡도**
- 시각화: 게시판 "누가 → 무엇 → 누구에게"(본사 관리자 → 본사 공지 → 전체 또는 지정 지점 / 지점 관리자 → 지점 공지 → 자기 지점 회원·직원, 열람은 전체 공지와 자기 지점 글만) / 혼잡도 5단계 구간표(현재 인원 ÷ 정원)와 수동 보정 흐름
- 시연: 본사·지점 공지 작성과 작성자 본인만 수정 / 인원 수동 보정 → 단계 즉시 재계산

**15. 자원문서관리** (분석서 도메인 04·05)
- 시각화: 취득가액 100만원 기준 소모품/고정자산 자동 분류 / 자산 상태 흐름(정상 → 수리중 → 폐기 대상 → 폐기됨, 수리 완료 시 정상 복귀) / 문서 보존기한 3종(인사서류 퇴사 후 3년 자동 · 계약서 직접 입력 · 매뉴얼·기타 영구)
- 시연: 자산 등록 시 자동 분류 → 상태 전환 / 문서 등록 → 보존기한 임박(D-16) 배지

### 마무리 (16~17장)

**16. 현재 상태 정직하게 + 다음 계획**

| 상태 | 항목 |
|---|---|
| ✅ 구현됨 | 9개 도메인 Phase 1 이상(예약·결제는 Phase 2 핵심까지), 역할별 화면 |
| 🔲 의도적 제외 | 슬라이드 8의 "뺀 것" — 근거·확장 방향 문서화 |
| ⬜ 미착수 | 회원 앱, 실DB 전환 |

다음 계획 후보: 회원 앱 착수 / 슬라이드 8의 "남긴 것"(RFP 잔여 항목) / 실DB 전환 여부 결정 / 계약서 OCR·AI(2주차부터 이어진 설계)

**17. 마무리 / Q&A**

### 시간 배분 (15분)

| 구간 | 슬라이드 | 시간 |
|---|---|---|
| 도입 | 1~3 | 2분 |
| 1부 실무 분석 | 4~6 | 3분 (6번이 핵심 — 1분 30초) |
| 2부 확장 판단 | 7~8 | 2분 30초 |
| 3부 도메인 시연 | 9~15 | 6분 (12·13 예약·결제는 각 1분, 나머지 도메인은 40~50초) |
| 마무리 | 16~17 | 1분 30초 |

초안(21장) 대비 줄인 방법(4장): 초안의 7·8번(확장 지도, 판단 프레임)을 **7번 한 장**으로 합치고, 초안의 9·10·11번(넣은 것, 뺀 것, 남긴 것)을 **8번 한 장의 3열 표**로 합쳤으며, 초안의 **19번(진척도 선택 슬라이드)을 삭제**하고 초안의 21번(마무리)을 17번(마무리/Q&A)에 흡수했다. 나머지 슬라이드는 번호만 당겨졌다.

## 3. 시각화 제작 계획 — 제작 완료(2026-09-21)

도식 9개를 **슬라이드 본문 영역 비율(1200×560)의 SVG**로 만들어 `assets/4-3_3주차발표/`에 저장했다(PowerPoint에 그대로 삽입 가능, 색은 고정값이라 슬라이드 배경과 상관없이 보인다). 표 3종(슬라이드 6·8·9)은 SVG가 아니라 미리보기 페이지의 HTML 표로만 만들었으니 슬라이드에서는 PowerPoint 표로 다시 짜거나 캡처해서 쓴다. 전체 미리보기: [3주차 발표 시각화](https://claude.ai/artifact/Qgrre75wywNGWSU8JarZ9c)(비공개 링크 — 공유 설정을 한 사람만 열 수 있음).

| 슬라이드 | 시각화 | 유형 | 파일 (`assets/4-3_3주차발표/`) |
|---|---|---|---|
| 4 | 6개 영역 → 한 영역의 3단 구조(예: 위탁계약) + 5-1 화면 캡처(미확보) | 도식 + 캡처 | `slide-04-analysis-structure.svg` |
| 5 | 사업 구조(계약·운영 두 관계) + 계약 상태 + 종료 시 막히는 3가지 | 흐름도 + 상태도 | `slide-05-business-map.svg` |
| 6 | 실무 → 설계 → 구현 추적표(동작하는 4행) | 표 | (페이지의 HTML 표) |
| 7 | 도메인 허브(RFP 8개 vs 신설) + 확장 판단 4질문 | 허브 + 흐름도 | `slide-07-domain-hub.svg` |
| 8 | 넣은 것 / 뺀 것 / 남긴 것 | 3열 표 | (페이지의 HTML 표) |
| 9 | 4개 역할 × 접근 범위 + 요청이 통과하는 3관문 | 표 + 흐름도 | (표는 HTML) `slide-09-guard-path.svg` |
| 10 | 채용·파견 / 출근 판정 / 휴가·연차 (3구간) | 흐름도 | `slide-10-hr-attendance.svg` |
| 11 | 지점 하위 데이터 + 프로그램 상태 전이 | 구조 + 상태도 | `slide-11-member-program.svg` |
| 12 | 예약 흐름 + 취소 정책(24시간) 타임라인 | 흐름도 + 타임라인 | `slide-12-reservation.svg` |
| 14 | 게시판 누가→무엇→누구 + 혼잡도 5단계·수동 보정 | 흐름도 + 구간표 | `slide-14-board-congestion.svg` |
| 15 | 자동 분류 + 자산 상태 흐름 + 문서 보존기한 | 도식 + 규칙표 | `slide-15-asset-document.svg` |

검증 범위: 도식의 상태·전이·구간값은 2026-09-21 코드에서 직접 확인했다. 글자 겹침·영역 이탈은 코드로 전수 검사했다. PowerPoint에서 SVG를 열었을 때의 글꼴 대체(IBM Plex Sans KR → 맑은 고딕)로 글자 폭이 달라질 수 있어 삽입 후 확인이 필요하다. 라이트 테마·휴대폰 폭의 미리보기 페이지는 눈으로 확인하지 않았다.

## 4. 시연 스크린샷 촬영 목록 (미확보)

2주차 스크린샷 폴더(`assets/4-2_2주차발표/`)는 저장소에 없고, 이번 화면들도 하나도 확보되지 않음. 촬영 시 계정: 정하늘(본사) · 김민수(서초점 지점관리자) · 박서연(직원) · 이수진(회원). 로그인은 로그인 화면의 데모 계정 카드로 하며, 15분 분량이므로 **필수**부터 확보한다.

- **[필수]** 5-1 기업 분석 자료 전체 화면 + 도메인 하나(예: 위탁계약 — 슬라이드 4 도식의 예시와 맞춤)의 3단 구조 확대 캡처 (슬라이드 4)
- **[필수]** 사이드바 역할별 비교(본사 vs 지점 관리자 vs 회원) (슬라이드 9)
- **[필수]** 근태: 체크인 → 휴가 신청 → 승인함 → 잔여연차 (슬라이드 10)
- **[필수]** 예약(회원): 회차 목록 → 결제 대기 → 확정 → 취소 / 예약(지점 관리자): 예약 현황 + 결제내역 (슬라이드 13)
- 회원: 등록 모달(보호자 동의 체크) → 상세 (슬라이드 11)
- 프로그램: 등록 모달 → 상태 드롭다운 (슬라이드 11)
- 게시판: 글쓰기 모달 → 상세(수정/삭제 버튼) (슬라이드 14)
- 혼잡도: 보정 전후 카드 (슬라이드 14)
- 자산: 등록 모달 → 목록 / 문서함: 임박 배지 (슬라이드 15)

## 5. 발표 전 확인할 것 (Q&A 대비 포함)

1. **5-1이 아직 "구현 안 하기로 한 것"을 설계 결정으로 서술하고 있음.** 분석서 본문에 결근 자동판정 배치, 강사 정산·원천징수, 계약 종료 60일 전 자동 갱신임박 전환 배치가 "설계 결정"으로 적혀 있고, 말미 "설계 현황" 표에는 예약·결제가 "매출 인식·강사 정산까지 확장"으로 돼 있다. 이 중 정산·자동 배치는 2026-09-20에 구현 범위에서 제외했고(갱신임박 자동 전환도 현재는 시드 데이터의 고정 상태값), 분석서의 서술과 어긋난다. **결정(2026-09-20): 분석서는 그대로 두고 (b) 프레임으로 설명한다** — "분석서에는 실무 전체를 명시하되, 구현을 진행하면서 불필요하다고 판단한 것은 제외하는 방향으로 진행했다." 슬라이드 6·8에서 이 문장을 그대로 쓰고, 제외한 항목은 [2-3 추적표 §2-4](../2.decisions/60_분석및제안/2-3_요구사항추적표.md)에 근거와 함께 남아 있다고 안내한다. 질문이 나오면 "분석서는 실무 지도, 구현 범위는 별도 판단"으로 답할 것.
2. **분석의 근거 범위.** 5-1은 원본 RFP와 일반적인 한국 실무 관행(고정자산 기준, 재물조사 절차, 법정 보존연한 등 — [1-10 참고자료](../1.spec/40_자원문서관리/1-10_기업구조및자원관리분석.md#참고자료))을 조사해 **재구성한 것**이고, 스포이즘의 실제 내부 자료·수치가 아니다. 지점 분포·계약 현황 같은 수치는 mock 데이터라 분석 근거로 쓰지 않았다. "실무를 직접 조사·분석해 설계로 연결했다"로 말하면 정확하고, "스포이즘 내부 실무를 안다"로 말하면 과장이 된다 — 질문이 나오면 이 구분으로 답할 것.
3. 지역관리자(SV) 계층은 분석에서 발견했지만 RFP에 없어 **일부러 설계에 넣지 않은** 사례 — 확장 판단의 좋은 예시로 슬라이드 7·8에서 언급할 수 있다.
4. **계약 종료 시 막아야 하는 5곳 중 구현은 3곳.** 슬라이드 5는 동작하는 3가지(신규 회원 등록·예약·게시글 작성)만 보여주고, "5곳을 막는다"고 말하지 말 것. 지점 소속 계정(정책 미정)과 파견 직원 종료 처리(미구현) 2곳은 슬라이드 8의 "남긴 것"에 있다. 질문이 나오면 "무엇이 남았는지까지 분석했고, 계약 상태를 바꾸는 API와 함께 구현할 계획"으로 답한다. 근거: [2-3 추적표 §2-3](../2.decisions/60_분석및제안/2-3_요구사항추적표.md).
5. **(슬라이드에는 넣지 않고, 질문이 나올 때만) 검증·테스트 관련.** 2026-09-21 결정으로 가드레일·테스트·CI는 이번 발표 슬라이드에서 제외했다. 다만 질문 대비로 사실만 정리해 둔다.
   - *"자동 테스트가 있나요?"* → 핵심 규칙 3개 영역(지점 데이터 격리·계약 종료 지점 차단·인사 권한 분리)에 HTTP 통합 테스트 119건이 있고 CI(GitHub Actions)가 push마다 lint·빌드·테스트를 돌린다. **그 밖의 도메인 로직(예약·결제 계산, 근태, 자산 등)과 admin-web 화면에는 자동 테스트가 없어 lint·빌드까지만 검증된다.** 전체가 검증됐다고 말하지 말 것.
   - *"지점 격리는 정말 막히나요?"* → 자동 테스트로 확인했고, 그 과정에서 검사가 빠져 있던 라우트(프로그램 회차 조회) 1건을 찾아 수정했다. 격리는 가드와 컨트롤러별 검사의 조합이라 새 라우트에서 누락될 수 있어, 공통 가드로의 중앙화는 실DB 전환 이후에 하기로 했다([2-3 추적표 §3 6번](../2.decisions/60_분석및제안/2-3_요구사항추적표.md)).
6. **(슬라이드에는 넣지 않고, 질문이 나올 때만) 부가세 분리.** 2026-09-21 결정으로 발표 슬라이드(6·7·8·12·13)에서 뺐다. 구현이 작고(결제 승인 시 `공급가액 = round(결제금액 ÷ 1.1)`, 부가세는 나머지) 결제 자체가 모의 결제라, 핵심 사례(계약 종료 차단, 본사 채용·파견, 자원문서관리 신설)보다 설득력이 약하다는 판단이다. 질문이 나오면 사실만 답한다.
   - *"부가세는 어떻게 처리하나요?"* → 결제 승인 시 총액에서 공급가액과 부가세를 역산해 결제내역에 나눠 기록한다(예: 30,000원 = 27,273 + 2,727). 다만 **모의 결제 위에서의 구현**이고, **프로그램 종류와 무관하게 일괄 10%를 적용**한다. 과세·면세 구분은 다루지 않으며, 실제 세무 처리는 범위 밖이다.

## 6. 참고 자료 (슬라이드 제작 시 인용)

- [5-1_기업분석자료.html](../5.deliverables/5-1_기업분석자료.html) — 실무 분석서 본문
- [2-3_요구사항추적표.md](../2.decisions/60_분석및제안/2-3_요구사항추적표.md) §2-2(도메인별 현황), §2-3(계약 종료 파견 처리 미구현), §2-4(범위 제외 결정)
- [1-7_예약및결제.md](../1.spec/20_이용자서비스/1-7_예약및결제.md) §6 — 동시성·취소 정책
- [1-10_기업구조및자원관리분석.md](../1.spec/40_자원문서관리/1-10_기업구조및자원관리분석.md) §7 — 확장 가능성 목록
- [2-1_기술결정사항.md](../2.decisions/50_결정및이슈기록/2-1_기술결정사항.md) D6(동시성), D10(혼잡도)
- [5-2_개발작업계획서.html](../5.deliverables/5-2_개발작업계획서.html) — 도메인 허브 다이어그램 재사용 후보

### 이번 발표 범위 커밋 (15개, 오래된 순)

| 커밋 | 날짜 | 내용 |
|---|---|---|
| `4f0fe5f` | 2026-09-17 | 문서 구조 2차 카테고리 재편 + 실무 밀도 보완 + RFP 산출물 2건 작성 |
| `67ce09f` | 2026-09-18 | RFP 산출물 2건을 도메인 구조·시각화 중심으로 재구성 (**5-1 실무 분석서 포함**) |
| `6c29af9` | 2026-09-18 | Phase 1 검토 수정 + 근태관리 구현 + 역할별 사이드바 개편 |
| `203cc26` | 2026-09-18 | Phase 1 수정·근태관리 반영해 현황 문서 정정 |
| `cb2540a` | 2026-09-18 | 회원관리 등록/수정/상세 화면 |
| `0e7468e` | 2026-09-18 | 강사프로그램게시 화면 (강사 CRUD + 프로그램 등록/수정) |
| `39e6185` | 2026-09-18 | 게시판 구현 |
| `d7938ce` | 2026-09-18 | 게시판 문서 반영 + 강사프로그램게시 완료 표시 정정 |
| `d1483e1` | 2026-09-18 | 혼잡도관리 Phase 1 |
| `0ef930e` | 2026-09-18 | 혼잡도관리 문서 반영 |
| `049097e` | 2026-09-18 | 예약및결제 Phase 1+2 핵심 |
| `7f3c0c4` | 2026-09-18 | 예약및결제 문서 반영 |
| `b42b13a` | 2026-09-19 | 자원문서관리 Phase 1 |
| `439c2ca` | 2026-09-20 | 자원문서관리 문서 반영 |
| `abf8caf` | 2026-09-20 | 쓰이지 않는 기능 범위 제외 + 제외 항목 문서화 |

발표 범위 밖(2026-09-21 결정으로 슬라이드 제외): `abf8caf` 이후의 lint 복구·CI·통합 테스트·격리 누수 수정·프로젝트 가드레일·admin-web lint 부채 정리 커밋. 질문 대비 사실은 §5-5번.

## 7. 다음 단계

1. ~~이 초안을 검토·확정~~ **완료(2026-09-21)** — 17장 / 15분, 새 성과는 슬라이드 제외
2. ~~시각화 제작(§3)~~ **완료(2026-09-21)** — 도식 9개(SVG), 표 3종(미리보기 페이지). 청중 기준으로 간추림, 부가세 제외
3. 시연 스크린샷 촬영(§4) — **필수** 항목부터
4. 슬라이드 비주얼 디자인 (SVG 삽입 후 글꼴·글자 폭 확인, 슬라이드 6·8·9의 표는 PowerPoint 표로 재구성)
