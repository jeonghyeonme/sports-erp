# 진행 로그

> **상태**: 진행 중. 이 문서는 "무엇을 만들었나"가 아니라 **"왜 이렇게 일하게 됐고, 무엇을 근거로 정했으며, 어떻게 검증했는가"**를 시간 순서로 남긴다.
> ADR([domains/*.md](../domains/))이 "이 결정 하나"를 기록한다면, 이 문서는 **결정들 사이의 인과관계와 이 프로젝트의 작업 방식 자체가 바뀐 지점**을 기록한다. 도메인 사이클을 하나 끝낼 때마다 여기에 한 단락을 추가한다([03_도메인_사이클_템플릿.md](./03_도메인_사이클_템플릿.md) §12에 반영).
> **읽는 법**: 각 단락은 "계기 → 한 일 → 근거/수치 → 결과" 순서다. 수치는 출처를 반드시 표시한다(실측/계산/가정).

## 1. 왜 "기능 구현"에서 "요구사항→Driver→검증"으로 방향을 틀었나

처음 요청은 "도메인별로 엣지케이스를 최대한 뽑아서 완성하자"였다. 실행해 보니(예약및결제 도메인 시범) 엣지케이스 목록 자체는 얼마든지 늘릴 수 있지만, **어떤 엣지케이스가 중요한지 판단할 기준이 없었다** — 그냥 감으로 "이것도 있고 저것도 있고"를 나열하는 상태였다.

사용자가 유튜브 영상 두 개를 제시했다:
- [AI에게 검증가능한 목표 제시하기](https://www.youtube.com/watch?v=OTttvhEHncQ) — 핵심 문장: "표준(ISO/IEC 25010·25023)은 무엇을 잴지 정의하지만, **목표값은 표준이 아니라 우리가 정한다.**"
- [티켓 예매 시스템 — 고객 요구에서 설계로](https://www.youtube.com/watch?v=7excI-0n2xk) — 핵심 흐름: 요구사항 분류(기능/품질/제약/**불변규칙**) → 모호함 되묻기 → 검증 가능한 형태로 구체화 → **Architecture Driver 식별(소수로 제한)** → 우선순위 → 설계 질문 → 대안·트레이드오프 → 결정(ADR) → 검증.

두 영상 다 자막을 전문 확인했다(YouTube 자동 자막 API로 직접 추출 — 처음엔 캡션 URL 서명이 만료돼 실패했고, 페이지를 다시 받아 재시도해 성공했다). **이게 이 프로젝트 전체 방법론의 출처다.** "엣지케이스를 많이 뽑는다"가 아니라 "요구사항을 분류하고, 그중 구조를 바꾸는 소수(Driver)만 골라 수치 근거로 대안을 비교한다"로 바뀌었다.

## 2. RFP를 다시 읽고 분류했다 (01번 문서)

`docs/제안요청서 원본.pdf` 11쪽을 pypdf로 전문 추출해 **문장 65개**를 기능(F)/품질(Q)/제약(C)/프로세스(P)/배경(B)으로 분류했다.

**핵심 수치**: 품질(Q) 20개 중 **14개가 판정 불가**(×), 수치가 있는 문장은 CPU 90%·메모리 85%·혼잡도 30분 셋뿐이고 그마저 측정 조건이 빠져 있었다. → "이 RFP는 기능은 명확하고 품질 목표는 비어 있다"는 결론. 이게 영상의 "목표값은 우리가 정한다"는 문장과 정확히 들어맞아, 이후 작업의 정당성 근거가 됐다.

## 3. 규모를 산정했다 (Q-01, 공용 산정 상수)

발주처에 다시 물을 수 없어 "임시 가정 + 확정 근거"로 진행하기로 했다(RFP §Ⅴ-7 "불분명한 사항은 협의해 결정"을 근거로 삼음). 사용자가 확정한 3가지: ①실측 데이터 없음(전부 산정) ②3년 후 목표치 기준 ③대표 업장 1,000세대.

산정 체인(**출처 유형을 매 단계 표시**):
1,000세대 × 2.2명/가구(**실측**: 2023 인구주택총조사 가구원수 분포 가중평균) = 2,200명 → × 25% 등록률(**가정, 약함**) = 550명 → × 36% 일일방문율(**가정**) = 200명/일 → Little's Law(L=λW, **계산**) + 피크계수 2.5배(**가정**) = 피크 동시 재실자 33명/지점 → 98개 업장(현재) 3,200명 → 130개 업장(3년 후, 연 10% 성장 **가정**) 4,300명.

이 값은 [architecture/design-constants.md](../architecture/design-constants.md)에 딱 한 번 만들어 두고, 이후 모든 도메인이 재사용한다(도메인마다 새로 산정하지 않는다는 원칙).

## 4. "9개 도메인, 감당이 안 된다" — 부담을 어떻게 줄였나

Q-01 하나에서 약한 가정이 3개(등록률·방문율·성장률) 나온 걸 보고, 사용자가 "9개 도메인에 이 깊이를 다 적용할 자신이 없다"고 했다. 대응은 두 가지:

1. **Tier1(정밀, 2~5개)/Tier2(경량, 규칙표)로 깊이를 나눈다.** 영상 자체가 이 원칙을 증명한다 — 티켓 시스템 예시도 Driver 5개, 설계 질문 3개로 끝났다(전체 요구사항을 다 이 무게로 다루지 않음).
2. **RFP 65개 문장을 9개 도메인에 매핑하고 상/중/하로 우선순위를 매겼다(02번 문서).** 기준: ①RFP 원문이 명시적으로 강조 ②CLAUDE.md 4대 불변식과 직결 ③사업 존속 리스크(B01: 불안정→민원→계약해지)와 직결. **결과: 상 4개(권한관리·인사정보관리·회원관리·예약및결제), 중 3개, 하 2개.** 상위 4개가 CLAUDE.md의 4대 불변식과 정확히 겹쳤다 — 기준이 이미 프로젝트가 암묵적으로 합의해 온 것과 일치한다는 신호로 받아들였다.

## 5. 도메인 사이클 템플릿을 확정했다 (03번 문서)

영상의 11단계를 도메인 하나에 반복 적용하는 틀을 만들었다. 두 가지를 도중에 고쳤다(둘 다 사용자 피드백):
- **레퍼런스(데이터모델·API·화면)는 결정의 결과물이라 문서 맨 아래(부록)에 둔다** — 위에 두면 "옛 스펙이 고정 사실이고 그 위에 결정을 얹는다"는 인상을 줘서, 실제로는 반대(Driver 분석이 스펙을 바꿀 수 있어야 함)라는 사용자 지적을 반영.
- **단, 사용자 스토리는 예외로 맨 앞에 둔다** — 스토리는 결정으로 바뀌지 않는 원본 입력값이라, 3자가 F01a 같은 ID를 만나기 전에 먼저 맥락을 잡게 하기 위함(영상도 고객의 원문 요구사항을 먼저 나열한 뒤 분류로 들어감).

## 6. 문서 구조를 실제로 재편했다

콘텐츠 3종류(도메인 지식/횡단 지식/과정 기록)를 3개 폴더(`domains/`/`architecture/`/`process/`)로 나누고, "병합 후 원본 유지"가 아니라 **"병합 후 원본 삭제(단, inbound 링크 정리 후)"**를 기본값으로 삼았다(사용자가 "유지는 최대한 피하라"고 명시). `docs/requirements/`(임시 작업 폴더)를 폐지하고 파일 6개를 이동, 상호 참조를 전부 고쳐 **링크 검증 스크립트로 0건 깨짐을 확인**했다(이후 매 사이클마다 재확인).

## 7. 4개 상위 도메인 사이클을 실행했다

권한관리 → 회원관리 → 인사정보관리 → 예약및결제 순서(의존관계 기준: 권한관리가 다른 모든 도메인의 게이트).

**사이클마다 코드를 실제로 열어봐서 발견한 문서-코드 괴리 3건**(감이 아니라 코드 대조로 확인):
- 회원관리: 옛 스펙이 "담당 직원 지점 검증 미구현"이라 적어 뒀지만 실제로는 구현돼 있었다(`assertStaffInBranch`).
- 인사정보관리: 코드 주석이 "파견발령 후 재로그인 필요"라고 적어 뒀지만, 실제 `JwtStrategy`는 매 요청 재조회라 재로그인 불필요했다(**코드 주석도 코드 자체보다 낡을 수 있다**는 사례).
- 인사정보관리(01번 문서 정정): Q-20의 임시 가정("본사 승인 필요")이 실제 1-3문서 결정("BRANCH_ADMIN 1단계 승인")과 달라 정정.

**도메인 간 패턴 재사용도 확인됐다**: "부분 unique 인덱스로 활성 상태만 유일성 강제"가 예약및결제(중복예약 방지, 원조) → 회원관리(ADR-MEM-02, 탈퇴 이메일 재사용) → 인사정보관리(ADR-STF-03, 활성 파견 유일성)로 **세 번 재사용**됐다. 우연이 아니라, 같은 문제 유형(활성 상태만 유일해야 함)을 도메인마다 다른 방식으로 풀지 않겠다는 원칙이 실제로 지켜진 것.

**가장 큰 발견**: 예약및결제는 이 프로젝트에서 가장 정교하게 설계된 도메인(1-7문서 §6의 "락 대상 정정" 논의)인데, **자동 테스트가 0건**이었다. 설계 품질과 검증 수준이 반비례하는 상태였다.

## 8. 발견한 구현 공백을 실제로 메웠다 (2026-09-22)

사용자가 "상인 것들까지만 문서 뽑고 구현 진행하자"고 정한 뒤, 4개 도메인 문서의 "다음 행동 제안" 중 **이미 구현된 로직에 테스트만 없는 항목**(저비용·고가치)을 우선 처리했다.

**테스트 파일 4개, 140개 테스트 전부 통과**:
- `auth-lifecycle.spec.ts`(권한관리): 퇴사 즉시 401, Role 전환 즉시 반영 — 2건
- `staff-assignment.spec.ts`(인사정보관리): 파견 발령 접근권 전환, 활성 파견 1건 유지, 퇴사 후 로그인 차단 — 4건
- `members-validation.spec.ts`(회원관리): 미성년 동의, 담당직원 지점일치, 전화번호 중복 경고 — 10건
- `reservation-capacity.spec.ts`(예약및결제): **정원 초과 방지**, 중복예약 방지, 모의결제 흐름, 취소 24시간 분기, VAT 계산 — 7건

**정원 초과 방지 테스트의 구체적 수치**: 신규 개설(예약 0건) capacity=2 회차에 서로 다른 회원 3명이 `Promise.all`로 동시 요청 → **정확히 2건 성공(201), 1건 409 SLOT_FULL**. 이 시나리오용 슬롯(`slot-seocho-yoga-2`, capacity=2)은 코드에 "SLOT_FULL 케이스를 바로 테스트할 수 있게" 라는 주석과 함께 이미 시드돼 있었는데, 그동안 쓰는 테스트가 없었다 — 이번에 처음 사용했다.

**VAT 계산 검증**: price 30000원 → `supplyAmount = round(30000/1.1) = 27273`, `vat = 30000-27273 = 2727`. 코드 그대로 재현되는지 확인.

**작업 중 발견한 환경 문제**: `apps/api/node_modules`가 아예 없고 `supertest`가 미설치 상태라 8개 테스트 스위트 전부가 실행 전에 실패했다 — `npm install`로 해결(제 코드 문제가 아니라 환경의 부분 설치 상태였음).

**검증 결과**: `npm run test`(8 suites, 140 passed) / `npm exec -- eslint .`(0 errors) / `npm run build`(성공). 4개 도메인 문서의 §11(검증 현황) 표를 "검증되지 않음"에서 "검증됨(2026-09-22) — 파일명"으로 갱신했다.

## 9. 방법론을 스킬로 일반화하고 이 프로젝트로 역이식했다 (2026-09-23)

사용자가 "이 방법론을 어느 프로젝트에서든 쓸 수 있게 만들고 싶다"고 요청해, §1~8에서 실전 검증한 절차(요구사항 분류·되묻기·수치화·Driver·ADR·검증·구현작업원칙·진행로그·문서구조)를 이 프로젝트 종속 내용을 뺀 채로 별도 저장소(`C:\Users\parad\architecture-driver-skill`, 별도 세션에서 GitHub/CI/훅 부트스트랩까지 확장됨)에 Claude Code 스킬로 추출했다. 그 스킬을 이 프로젝트 `.claude/skills/architecture-driver/`에 설치했다(`infra/`는 이 프로젝트에 이미 GitHub·CI·훅이 있어 제외 — SKILL.md·references/·assets/templates/만 복사).

**이 스킬 설치 자체가 방법론의 정확도를 검증하는 계기가 됐다**: 스킬의 Bootstrap 1~4단계(구현 작업 원칙 블록, docs/domains·architecture·process 분리, 진행 로그)가 이미 이 프로젝트에 전부 갖춰져 있었다 — 당연한 결과다, 이게 원본이니까. 스킬 저장소의 "복사, 링크 아님" 원칙(`infra/bootstrap-checklist.md` "Version tracking")에 따라 CLAUDE.md에 `architecture-driver-skill @ 7416458`(2026-09-23) 버전 추적 라인을 남겼다.

## 10. 스킬을 ERP 프로젝트에 설치하고 근태관리에 시험 적용했다 (2026-09-23)

`.claude/skills/architecture-driver/`에 설치(`infra/`는 이 프로젝트에 이미 GitHub·CI·훅이 있어 제외). CLAUDE.md에 `architecture-driver-skill @ 7416458` 버전 추적 라인을 남겼다. Bootstrap 1~4단계(구현 원칙 블록·docs 분리·진행 로그)는 이 프로젝트가 원본이라 이미 전부 갖춰져 있었다 — 스킬 정확도의 간접 검증.

**중 우선순위 첫 도메인 근태관리를 스킬의 `domain-doc-template.md`로 만들었다**(손으로 만든 이전 4개와 다른 산출 경로). Driver 3개:
- ADR-ATT-01(연차 승인 시점에만 차감 — 재확인)
- ADR-ATT-02(결근을 스케줄러 배치 대신 조회 시점 지연 계산으로 표현 — 1-4문서가 이미 제안해 둔 확장 경로)
- ADR-ATT-03(근태 기록의 지점 귀속을 체크인 시점 `branchId` 스냅샷으로 — **이번 사이클에서 새로 발견한 실제 버그**: 월중 파견 이동 시 과거 근무 기록이 잘못된 지점 요약에 집계되는 소급 왜곡. 코드를 실제로 추적해서(`attendanceSummary`가 현재 `Staff.branchId`로만 필터) 확인했다)

**또 하나의 01번 문서 정정**: Q-21의 임시 가정("연차 법정 산정은 범위 외")이 틀렸다 — 실제로는 근로기준법 단순화 공식이 이미 구현돼 있었다(`calcAnnualLeaveTotalDays`). 반대로 Q-21의 "파견 이동 시 근태 귀속" 부분은 코드가 정말로 안 다루고 있어서 ADR-ATT-03으로 이어졌다 — 같은 되묻기 항목 안에서 "가정이 틀린 절반"과 "가정이 맞았던(문제가 진짜였던) 절반"이 공존한 사례.

**ATT-T05/STF-T04 교차 확인**: `offDays`(정기 휴무 요일) 필드 부재가 인사정보관리·근태관리 두 도메인 사이클에서 독립적으로 발견됐다 — 같은 필드 하나가 두 도메인의 다음 작업을 막고 있다는 게 이번에 명시적으로 연결됐다.

## 11. 사용자 피드백으로 ADR-ATT-02를 재검토하고, 날짜 경계 버그를 고쳤다 (2026-09-23)

**계기**: 사용자가 근태관리 문서를 보고 세 가지를 지적했다 — ① 결근이 "조회 시점 계산"만으로는 이력이 안 남는 것 아니냐 ② 체크인에 GPS/VPN 같은 우회 엣지케이스가 있지 않냐 ③ 다른 도메인에도 비슷한 우회·휴먼에러가 있을 것.

**① 결근 이력**: 맞는 지적이었다. ADR-ATT-02를 "조회 시점 지연 계산"에서 **관리자 명시적 "결근 확정" 액션**으로 재결정했다 — ADR-AUTH-02(되돌리기 어려운 인사 조치는 사람이 트리거)와 같은 원칙을 재사용했다. 구현은 §12에서 이어짐.

**② GPS/VPN**: 스펙이 이미 위치 검증 자체를 빼뒀어서(1-4문서 §0) 해당 없음 — 속일 대상이 없다. 대신 진짜 남는 잔여 위험(대리출근/공유계정)을 ATT-T06으로 명시 기록했고, 이미 잘 막혀 있던 것(서버 시각 사용, 중복 체크인 차단)도 코드로 재확인했다.

**③ 횡단 결함 — 실측**: `new Date().toISOString().slice(0, 10)`로 "오늘 날짜"를 구하는 패턴을 grep했더니 **5개 도메인 10곳**(근태·회원·인사·게시판·자원문서)에 있었다. `toISOString()`은 서버 시간대와 무관하게 항상 UTC라, **매일 00:00~08:59 KST(9시간)** 동안의 모든 이벤트가 하루 전 날짜로 기록되는 구조적 버그였다 — 드문 경계 케이스가 아니라 매일 상시 발생. 도메인 하나 문제가 아니라서 [architecture/date-time-handling.md](../architecture/date-time-handling.md)로 분리해 대안 비교(ADR)를 하고, `apps/api/src/common/date/kst-date.ts`(`todayKst`/`toKstDateString`/`kstHoursMinutes`/`addYearsToDateString`, UTC+9 고정 오프셋, 환경변수 비의존)를 만들어 10곳 전부 교체했다. 근태관리의 `isLate()`(자동 지각 판정)도 같은 뿌리(호스트 시간대 의존)의 별개 버그라 같이 고쳤다.

**검증**: `kst-date.spec.ts` 7건(경계 시각 KST 00:30/23:59/00:00 정각 포함) + 기존 스위트 전체 — `npm run test`(9 suites, **147 passed**), `eslint`(0 errors), `nest build`(성공).

**CLAUDE.md 갱신**: "날짜 계산은 `kst-date.ts` 사용, `toISOString().slice(0,10)` 직접 금지" 규칙 추가 — 재발 방지가 목적(이 버그 자체가 "아무도 신경 안 쓴 설정 하나가 조용히 틀렸던" 사례였다).

**스킬 개선 후보로 승격**: `architecture-driver` 스킬의 `domain-cycle.md`가 Driver 식별을 "구조를 바꾸는가"로만 걸러서, 시간 경계·우회 가능성 같은 관점을 명시적으로 안 물어봐 처음엔 이 셋을 놓쳤다. 사용자가 정한 순서의 4번째 작업(§13 예정)에서 다룬다.

**커밋**: `3fa3a65`.

## 12. ADR-ATT-02(결근 확정)를 실제로 구현했다 (2026-09-23)

**계기**: 사용자가 "1번부터 순서대로 진행해줘"라고 정한 순서(①날짜 버그 ②ADR-ATT-02 ③ADR-ATT-03 ④스킬 개선)의 2번째. ADR-ATT-02는 `offDays`(ATT-T05/STF-T04, 인사정보관리·근태관리 두 사이클에서 각각 발견된 미구현 필드)가 선행돼야 한다고 이미 적어 뒀던 터라, 먼저 그것부터 만들었다.

**구현**: `MockStaff.offDays`(0=일~6=토, 파트타임은 항상 `undefined`) + `CreateStaffDto`/`UpdateStaffDto` 검증(정수 배열, 0~6 범위) → `mock-data.service.ts`에 `isWorkDay`(근무일 판정)·`previewAbsences`(잠정 결근 미리보기, **저장 안 함**)·`confirmAbsences`(관리자 확정 시에만 `AttendanceRecord(status=ABSENT)` 영속화, 재확정해도 중복 안 생김 — `previewAbsences`가 이미 기록 있는 날짜를 제외하므로 자연히 멱등) → `GET /attendance/absence-preview`(SUPER_ADMIN도 조회 가능)·`POST /attendance/absence-confirm`(**BRANCH_ADMIN만**, SUPER_ADMIN도 403 — "현장 운영 비개입 원칙"을 쓰기 액션에 실제로 적용한 사례).

**검증**: `attendance-absence.spec.ts` 7건(미리보기 비영속, 확정 영속+멱등, 승인휴가/체크인기록/파트타임 제외, 권한 2종) — `npm run test`(10 suites, **154 passed**), `eslint`(0 errors), `nest build`(성공). 근태관리·인사정보관리 두 도메인 문서의 검증현황·데이터모델을 함께 갱신(ATT-T05/STF-T04가 두 문서에 걸쳐 있었으므로).

**남은 것**: ADR-ATT-02의 "감수하는 것"(관리자가 확정을 잊으면 이력 공백)은 여전히 유효 — 시스템이 아니라 운영 절차로 완화해야 한다는 판단은 안 바뀜.

**커밋**: `eb992ce`.

## 13. ADR-ATT-03(지점 귀속 스냅샷)을 구현했다 — 스코프가 예상보다 넓어진 사례 (2026-09-23)

**계기**: 순서의 3번째. §12(ADR-ATT-02)를 만들면서 `previewAbsences`/`confirmAbsences`가 "현재 지점 소속 직원"만 후보로 삼는다는 걸 알고 있었는데, 그게 정확히 ADR-ATT-03이 고치려던 것과 같은 종류의 소급 왜곡이라는 걸 이번에 구현하면서 알아챘다 — **처음 이 ADR을 쓸 때는 `checkIn`/`attendanceSummary` 두 곳만 고치면 된다고 적었는데, 실제로는 그 사이에 들어온 ADR-ATT-02의 두 함수까지 포함해 세 경로를 다 고쳐야 했다.**

**구현**: `MockAttendanceRecord`에 `branchId`(기록 시점 소속 스냅샷) 필드 추가. `branchIdForStaffOnDate(staffId, date)` 헬퍼(파견 이력에서 그 날짜의 소속을 조회, `endDate`를 배타적으로 다뤄 파견 전환 당일은 새 지점 소속으로 판정 — ADR-AUTH-01 "즉시 반영"과 같은 원칙)를 만들어 `checkIn`·`previewAbsences`·`confirmAbsences`·`attendanceSummary` **네 곳이 전부 공유**하게 했다. `attendanceSummary`는 집계 기준을 "현재 이 지점 소속 직원"에서 "그 기록 자체의 branchId"로 바꿨고, 표에 뜨는 직원 명단도 "현재 소속"과 "이 지점 파견 이력이 있는 사람"의 합집합으로 넓혔다(전출한 직원의 전출 전 기록도 안 사라지게).

**검증**: `attendance-branch-snapshot.spec.ts` 3건 — 서초→강남 파견 이동을 고정된 과거 달(2026-06)로 재현해, 결근 확정이 전/후로 정확히 갈리고(겹침 0건) 겹치지 않는지, 체크인은 현재 소속으로 찍히는지, 지점 요약이 기록 자체의 branchId를 따르는지 확인했다(전출 전 기록이 예전 지점 요약에만 보임). 처음 작성한 assertion에 버그가 있었다 — 서초 관리자 본인(`staff-minsu`)도 셀프서비스로 같이 잡히는 걸 안 걸러서 실패했고, `staffId` 필터를 추가해 고쳤다(서비스 로직 문제가 아니라 테스트 문제였음, 정직하게 기록). `npm run test`(11 suites, **157 passed**), `eslint`(0 errors), `nest build`(성공).

**남은 것**: 없음 — 감수하는 것 항목이 전부 해소됐다.

**커밋**: `2c387be`.

## 14. 스킬에 "시간경계·우회·인간실수" 체크와 "결정 재검토" 가이드를 추가했다 (2026-09-23)

**계기**: 사용자가 정한 순서의 마지막(4번째). §11~13에서 실제로 두 번 겪은 일 — ① KST 날짜 버그와 ADR-ATT-03(지점 귀속)은 둘 다 "Driver 식별" 단계에서 "구조를 바꾸는가"만 물었으면 처음부터 못 잡았을 것들이다(코드를 직접 추적하거나 사용자가 물어봐서야 드러남). ② ADR-ATT-02는 사용자 지적으로 중간에 결정 자체가 뒤집혔는데, 스킬 문서 어디에도 "결정을 재검토해도 된다"는 말이 없었다. 둘 다 스킬 자체의 공백이지 이 프로젝트만의 우연이 아니라고 판단해 스킬 저장소(`architecture-driver-skill`)를 직접 고쳤다.

**변경**: `references/domain-cycle.md` 4단계(Driver 식별)에 "목록 확정 전 짧은 체크"를 추가했다 — 시간 경계·동시성·우회 가능성(스펙이 이미 뺀 검증 수단을 속이는 건 위협이 아니라는 주의사항 포함)·인간 개입 실수·입력 경계 5개 관점을 Driver 후보와 "미승격" 규칙표 양쪽에 통과시킨다. 8단계·10단계에는 "이미 내린 결정도 절대적이지 않다"는 재검토 가이드(예전 표는 남기고 새 정보·이유를 적은 뒤 재결정, ADR 제목에 "— revised" 표기)를 추가했다. `SKILL.md`가 이 체크를 가리키게 하고, `CHANGELOG.md`에 근거(이 프로젝트에서 실제로 놓쳤던 두 사례)를 남겼다.

**원본-스킬 동기화**: CLAUDE.md가 "원본은 이 프로젝트"라고 못박아 둔 원칙대로, 스킬 저장소에 쓴 내용을 [docs/process/03_도메인_사이클_템플릿.md](./03_도메인_사이클_템플릿.md)에도 같은 취지로 반영(한국어, 이번 근태관리 사례를 직접 인용)한 뒤, 스킬 저장소를 커밋(`7bca3fa`)하고 그 커밋의 `SKILL.md`·`references/domain-cycle.md`를 이 프로젝트의 `.claude/skills/architecture-driver/`에 다시 복사했다. CLAUDE.md의 버전 추적 라인도 `7416458` → `7bca3fa`로 갱신.

**검증**: 문서만 바뀌었다(코드 변경 없음) — 링크 검증 27개 파일 0건 깨짐.

**"1번부터 순서대로" 네 항목 전부 완료.**

## 15. 남은 중/하 우선순위 3개 도메인(강사프로그램게시·혼잡도관리·게시판) 사이클을 실행했다 (2026-09-23)

**계기**: 사용자가 "나머지 3개 도메인 사이클도 진행해줘"로 요청. §10~14에서 근태관리 1개 도메인에 스킬을 시험 적용해 KST 버그·ADR-ATT-02/03을 발견·수정한 뒤라, 같은 방식(11단계 + 4단계의 시간경계/우회/인간실수 체크)을 나머지 3개에 그대로 반복 적용했다. `architecture-driver` 스킬을 명시적으로 다시 불러 §14에서 추가한 체크리스트가 실제로 반영된 버전인지 확인 후 시작했다.

**한 일**: 옛 스펙 문서(1-8·1-9·1-5) 3개와 관련 컨트롤러·`mock-data.service.ts` 해당 함수들을 코드로 직접 대조해 각 도메인 3개(강사프로그램게시)/3개(혼잡도관리)/2개(게시판) Driver로 정리, [domains/강사프로그램게시.md](../domains/강사프로그램게시.md)·[domains/혼잡도관리.md](../domains/혼잡도관리.md)·[domains/게시판.md](../domains/게시판.md)로 작성했다. 옛 문서 3개에 대체 배너를 달고, [05_문서_마이그레이션_추적.md](./05_문서_마이그레이션_추적.md)·[02_도메인별_우선순위.md](./02_도메인별_우선순위.md)를 갱신했다.

**발견**: 코드를 직접 열어봤기 때문에 스펙만 읽어서는 안 보였을 격차가 드러났다 — 특히 **게시판의 `posts.controller.ts`는 `visibleToMember` 필드 자체가 타입에 없고, `list()`/`findVisibleOrThrow()`가 `SUPER_ADMIN` 여부로만 분기해 MEMBER·STAFF·BRANCH_ADMIN을 구분하지 않는다.** 즉 회원 계정으로도 직원 전용 HQ 공지(교육자료 등)가 그대로 조회된다 — 1-5문서 §7이 "회원에게는 비공개가 기본값"이라고 이미 정해뒀던 요구사항이 구현 단계에서 누락된 **실제 정보 노출 결함**이다(ADR-BRD-01로 기록, 아직 미수정). 혼잡도관리·강사프로그램게시 3개씩도 CLAUDE.md 핵심 불변식(계약종료 지점 차단)이 이 두 도메인에는 적용 안 돼 있었다는 동일 패턴의 격차를 포함한다.

**검증**: 문서만 작성했다(코드 변경 없음 — 이번 사이클은 근태관리 때와 마찬가지로 문서 우선, 구현은 별도 명시적 요청을 기다린다). 링크 검증 스크립트를 전체 `docs/*.md`(4.presentation·5.deliverables 제외)에 재실행해 깨진 링크 0건을 확인했다 — 실행 중 이번 작업과 무관한 기존 버그 하나를 추가로 발견해 수정했다: `2-1_기술결정사항.md`의 `app.module.ts`·`render.yaml` 상대링크가 `../../`로 한 단계 부족해 깨져 있던 것을 `../../../`로 고쳤다(사전에 존재하던 오류, 이번 3개 도메인 작업과는 무관).

**남은 것**: 강사프로그램게시 3개(ADR-PRG-01~03), 혼잡도관리 3개(ADR-FAC-01~03), 게시판 2개(ADR-BRD-01~02) — 총 8개 ADR 모두 문서만 있고 구현 전. 우선순위는 각 도메인 문서 §11에 정리했다. 자원문서관리(하 우선순위, 9번째 도메인)는 아직 착수 전.

## 16. ADR-BRD-01(회원 대상 공지 가시성 결함)을 수정했다 (2026-09-25)

**계기**: §15에서 발견해둔 8개 ADR 중 실제 정보 노출 결함인 ADR-BRD-01이 가장 급하다고 판단해 사용자가 우선 지정. "구현 작업 원칙"에 따라 [domains/게시판.md](../domains/게시판.md) §8의 기존 결정(대안 B — `visibleToMember` 플래그, 전면 차단 대신 선택적 공개)을 그대로 코드로 옮겼다.

**구현**: `MockPost.visibleToMember: boolean`(기본 false) 추가. `PostsController`에 `isVisibleTo(post, user)` 헬퍼를 만들어 `list()`와 `findVisibleOrThrow()`(상세·수정·삭제 공용)가 공유하게 했다 — MEMBER role일 때만 `scope==='BRANCH_TO_MEMBER' || visibleToMember===true`를 추가로 요구하고, STAFF·BRANCH_ADMIN은 기존과 동일하게 지점 소속만으로 판정(불변규칙은 "회원에게 비공개"이지 "직원에게 비공개"가 아니므로). `MockDataService.createPost`는 `BRANCH_TO_MEMBER`(지점 공지)는 scope 자체로 이미 회원에게 노출되므로 `visibleToMember`를 항상 true로 강제하고, `HQ_TO_BRANCH`(본사 공지)만 호출자가 지정한 값(미지정 시 false)을 쓴다. `CreatePostDto.visibleToMember`(옵션, SUPER_ADMIN만 의미 있음)를 추가하고, admin-web `BoardPage`의 작성 폼에 SUPER_ADMIN 전용 "회원에게도 공개" 체크박스를 달았다 — 플래그를 만들어놓고 UI에서 절대 true로 못 만들면 "모든 HQ 공지가 회원에게 영구히 안 보이는" 새로운 결함이 되므로, 최소 구현 범위에 이 체크박스까지 포함시켰다(수정 화면에서 토글하는 기능은 이번 범위 밖 — ADR이 다룬 건 작성 시점 판단이었다).

**부수 발견**: 구현 중 §15의 BRD-T03(계약종료 지점 신규 게시 차단) 진단이 오판이었음을 확인했다 — `createPost`의 컨트롤러 쪽만 보고 "차단 없음"이라고 적었지만, 실제 체크는 서비스 계층(`MockDataService.createPost`)에 이미 있었고 `contract-termination.spec.ts`가 이미 이 케이스를 통과시키고 있었다. 코드 구현 전에는 "테스트가 통과하면 검증됨"이 맞지만, **진단(구현 안 됨 판정) 자체도 테스트 실행이나 전체 호출 경로 추적 없이 컨트롤러만 읽고 내리면 틀릴 수 있다**는 사례로 기록해둔다. 문서(§6·§11·부록 A-3)를 정정했다.

**검증**: 새 회귀 테스트 `apps/api/test/posts-member-visibility.spec.ts` 7건 — 시드된 직원 전용 HQ 공지가 회원 목록/상세에서 빠지는지, 같은 공지가 본사관리자·지점관리자·직원에게는 보이는지(대조군), 회원이 본인 지점 지점공지는 그대로 보는지, `visibleToMember=true`로 작성한 HQ 공지가 회원에게 노출되는지, 플래그 미지정 시 기본값 false인지. `npm run test`(12 suites, **163 passed**, 기존 1 todo 포함 164), `eslint`(api·admin-web 둘 다 0 errors — api에 사전부터 있던 `main.ts`의 무관한 warning 1건 제외), `nest build`·`tsc -b && vite build` 둘 다 성공.

**남은 것**: ADR-BRD-02(페이지네이션)가 게시판 도메인의 마지막 미구현 ADR. 자원문서관리(9번째 도메인) 사이클은 여전히 미착수.

## 다음에 이 로그에 추가할 것

- 자원문서관리(9번째, 마지막 도메인) 사이클 착수 여부와 시점.
- 남은 7개 ADR(강사프로그램게시 PRG-01~03, 혼잡도관리 FAC-01~03, 게시판 BRD-02) 구현 여부와 시점.
- 회원관리 D3(회원 상세 요약 필드) 등 아직 미착수로 남은 구현 항목.
- 1.spec/2.decisions 옛 문서 삭제 실행(inbound 링크 정리) 시점.
