# Progress log

> This is not a changelog. Git history already records *what* changed. This
> file records *why each step was the right next move given everything
> decided so far* — the causal chain between decisions, and the points where
> the way this project is worked on changed. See
> `references/implementation-principle.md` for what makes a good entry.
>
> Add one entry per Domain Cycle completed (domain-cycle.md step 12) and
> whenever a piece of implementation work is done under the implementation
> principle. Read the most recent entries first when picking up this project
> in a new session — they're the fastest way back into context.

## <date> — <short title>

**Trigger**: <what prompted this — a user request, a finding from a previous
step, a discovered gap>

**What was done**: <brief>

**Basis / numbers** (label each: measured / calculated / assumed): <specifics
— cite file, formula, or figure, not vague claims>

**Result**: <what changed, what's now known, what's left open>
